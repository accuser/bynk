---
level: patch
changelog: Resolves #1479 — `tests_emit.rs`'s own no-body direct callees now build real `bynk_ts` trees instead of pre-printed `String`/`out: &mut String`: `emit_ns_destructure` (`-> Vec<TsStmt>`, printed by every caller via a new `crate::emitter::extend_printed_at(out, stmts, depth)` — `extend_printed_at` generalizes #1478's `extend_printed` with an explicit depth parameter, since this file's own scaffold-body callees print nested at depth 2, not `emitter.rs`'s module-level depth 0), `emit_test_deps`/`emit_integration_harness` (each returns the one real `TsStmt` they ever built, was pre-printed `String`), `observation_call_record_types` (`-> Vec<TsStmt>`), the codec-helper glue inside `emit_test_module` (now calls `serialisation::decls_as_stmts[_block]` + `extend_printed_at` instead of `serialisation::print_decls[_block]`), and `emit_system_http_support`/`sysdrive_driver` (`SystemHttpSupport.code` and `sysdrive_driver`'s return both go from pre-printed `String` to real `TsStmt`(s); the HS256 signer block, #1485's own separate scope, stays exactly the hand-written text it always was, carried forward as one `TsStmt::raw`). Converting the codec-helper glue's call sites removed `serialisation::print_decls`/`print_decls_block`'s own last two production callers — deleted both (dead code), fixing the stale intra-doc `[\`print_decls\`]` links elsewhere in `serialisation.rs` that referenced them. `emit_test_module`/`emit_integration_module` themselves are **not** converted by this issue, matching its own scope — that's the capstone, #1486, once every direct callee across both `emitter.rs`/`tests_emit.rs` has landed. Byte-identical output verified: `cargo test -p bynk-emit` (187/187), `cargo test -p bynkc` (full fixture-diff + e2e suite, including `tsc_verify`/`bless_positive_fixtures`/`positive_fixtures`). `ts_writes` (gated probe) unchanged at 829 — every converted call site here was already `print_stmt`-based, not `format!`/`write!`-based, so this slice moves construction style, not that particular count.
---
