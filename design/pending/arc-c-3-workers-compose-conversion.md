---
level: minor
changelog: "Arc C, slice 3: workers.rs's emit_worker_compose and its nine private helpers construct and return a real bynk_ts::TsProgram instead of writeln!-ing a String -- bynk-emit/src/project.rs's own compose.ts construction site drops its Verbatim/NotYetConverted wrap. Extends the bynk-ts node algebra with the five shapes Decision A named (a richer TsObjectEntry -- Prop/Shorthand/Method/Spread -- replacing TsExpr::Object's bare (String, TsExpr) entries; TsExpr::Arrow; TsExpr::OptionalMember/OptionalIndex; an optional flag on TsType::Object's fields) plus real gaps found during implementation beyond that list: TsDecl::Function and TsDecl::TypeAlias (compose.ts's own top-level function and its conditional DurableObjectNamespace fallback alias), TsDecl::ImportNamespace (the `import * as X` form), a parameterless TsStmt::TryCatch catch clause (ES2019 optional catch binding), and four new TsBinaryOp/one new TsUnaryOp operators (===, !==, &&, ||, typeof) with a precedence-aware fix to nested-Binary-operand parenthesisation. Adds a TypeRef -> TsType structural converter (ts_type_ref_qualified_ts_type, emitter.rs) alongside the existing String-returning ts_type_ref_qualified, and a TsExpr-returning twin of project.rs's own instantiate_provider_expr (instantiate_provider_ts_expr) for cross-context capability-provider construction -- both added rather than replacing their String-returning siblings, since other still-unconverted callers (emit.rs/lower.rs/serialisation.rs, emit_composition_root) still need them. Two cross-crate/cross-file String-producing dependencies genuinely out of this slice's scope (bynk_check::actors::claim_predicate_to_js; workers_entry.rs's own deserialise_call/brand_assertion, not yet converted) are carried as opaque pre-rendered text via TsExpr::Ident, the same precedent emitter.rs's own TypeRef::Query arm already established for TsType::Named. Zero output change: bless_positive_fixtures/positive_fixtures (all 116 compose.ts byte-golden fixtures) and tsc_verify's full strict-tsc corpus pass unchanged. verbatim_sites 10 -> 9 (this slice's one call site); ts_writes 1585 -> 1462 (workers.rs's own ~230 writeln!/write!/format! calls gone, plus one more from review's finding 3, a dotted-property Member fixed to a real member_chain); ast_importers stays at 5 as the accepted proposal's own Framing predicted (this file's *input*-side AST walk is unchanged, only its *output* representation converted). Review found three more real issues, all fixed before merge: TsExpr::Arrow was missing from every parenthesisation rule (callee, binary-operand, as-operand positions), not reachable by any real content today but closed anyway per the same #1314-finding-2 precedent, with three direct tests; the initial ts_any 30 -> 27 reading was a probe artifact, not a real reduction -- the three any's are still emitted byte-identically, only their *Rust-source spelling* changed from writeln!-string to TsType::named(\"any\"), which xtask's line_violates_ts_any predicate didn't yet match -- widened to also match the named(\"any\" construction spelling, restoring ts_any to its real, unchanged value of 30; and the env.<binding> Member at workers.rs:1423 was a single property node lying about its own name (a format!-built dotted string) rather than a real two-level chain -- fixed to use the file's own existing member_chain helper, which happens to be the one writeln!/format! call ts_writes's own final 1462 (not 1463) reflects. (#1321, #1322)"
---
