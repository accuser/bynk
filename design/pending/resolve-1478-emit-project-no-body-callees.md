---
level: patch
changelog: Resolves #1478 — the 12 "no-body direct callee"s `emit_project`/`emit` call at their own top-level signature now build real `bynk_ts::TsStmt` trees (`Vec<bynk_ts::TsStmt>`) instead of writing into a shared `out: &mut String`: `write_commons_doc`, `emit_context_rebrands`, `emit_cross_context_namespace_imports`, `emit_project_imports`, `write_header`, `emit_capability`, `emit_type` (plus its `emit_refined_type`/`emit_record_type`/`emit_sum_type` sub-dispatchers), `emit_messages_bundle`, `emit_make_surface`, `emit_boundary_helpers` (plus its `emit_consumed_context_helpers` sub-call), and `emit_json_codec_helpers`. Two still-`String`-typed unconverted siblings (`emit_context_deps_interface`, `emit_refined_checks`) are bridged the same way earlier slices already established: their output is captured into a local buffer and carried as one `bynk_ts::TsStmtKind::Raw` statement. A new `extend_printed(out, stmts)` helper in `emitter.rs` bridges each converted callee's `Vec<TsStmt>` back to the three call sites' own `out: &mut String` (`emit`, `emit_project`, and `emit_project`'s per-item loop) that still need `String` output at the top; `serialisation::decls_as_stmts`/`decls_as_stmts_block` (siblings of the existing `print_decls`/`print_decls_block`) let the boundary-helpers trio thread their real declarations through without an intermediate print. Byte-identical output verified: `cargo test -p bynk-emit` (187/187), `cargo test -p bynkc --test tsc_verify`, `cargo test -p bynkc` (full fixture-diff + e2e suite, including `bless_positive_fixtures`/`positive_fixtures`). `ts_writes` (gated probe) drops 851 → 831 — the real, intended movement from these 12 sites no longer writing through `format!`/`write!`.
---
