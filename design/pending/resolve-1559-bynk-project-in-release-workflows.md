---
level: patch
changelog: "`bynk-project` (a real dependency of `bynk-check`/`bynk-emit`/`bynk-ide`/`bynk-lower` since its 30 July 2026 extraction) is now published by both release workflows — it was missing from both hardcoded crate lists, which would have broken the next real release (#1559)"
---
