---
level: patch
changelog: Resolves #1485 — `emit_system_http_support`'s own HS256 JWT-signer block (`__bynkNow`/`__b64url`/`__bytesB64url`/`__bynkSignHs256`) moves from a raw Rust multi-line string literal (with `\`-continuations and no justifying comment) to a committed `emitter/test_runtime/jwt_signer.ts` file, spliced in via `include_str!` through a new `jwt_signer_runtime_helpers()` function — the same treatment `expectation_runtime_helpers`/`stub_runtime_helpers` (and the other `test_runtime/*.ts` files) already get. Found by #1472's own callee-cascade read as a genuinely new gap: unlike every other permanent site in this arc, this one had no in-place argument for staying hand-written text before this change. The `.ts` file's own content was extracted byte-for-byte from a real fixture's expected output (`bynkc/tests/fixtures/positive/379_system_http_boundary`) rather than hand-transcribed from the escaped Rust literal, to guarantee fidelity. `emit_system_http_support` still carries the signer as one opaque `TsStmt::raw` (unchanged) — only its own source moved. Verified: `cargo test -p bynk-emit` (187/187) and the full `cargo test --workspace` (all green, byte-identical fixture output via the `tsc_verify` corpus). `ts_writes`/`verbatim_sites`/`verbatim_origins` (gated probes) unchanged — `greenfield-status` needed no regeneration.
---
