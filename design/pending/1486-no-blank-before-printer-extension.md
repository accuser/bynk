---
level: patch
changelog: Part of #1486 (the `verbatim_sites` capstone) — `bynk_ts::TsStmt` gains a new `pub no_blank_before: bool` field (default `false`, threaded through every existing constructor, no behaviour change for any current caller — the full workspace test suite passes unchanged). `crate::printer::print`'s own top-level loop, which inserts exactly one blank line between every pair of adjacent top-level statements (its own "readability policy"), now also treats `next.no_blank_before` as an exemption alongside the existing import/comment/`ReExportAll`/`Verbatim` cases. Found necessary while planning #1486: converting `emit_project`/`emit_test_module`/`emit_integration_module` to return a real, unwrapped `TsProgram` (dropping their callers' single opaque `TsStmt::verbatim` wrap) exposes every constituent callee's own internal multi-statement spacing directly to this automatic policy for the first time — and `emit_agent`'s own already-shipped `registry`-then-`zero-factory` pairing (#1482, predating this field) genuinely needs zero blank lines between two adjacent real statements, a shape the printer's own uniform policy cannot express without a per-statement escape hatch. Confirmed empirically (a temporary test proved the printer inserts an unwanted blank between a `const` and a `function` with no exemption available) before choosing this fix over the alternatives (accepting bounded fixture format drift, or gaming the `verbatim_sites` probe by tagging permanent multi-statement content `Raw` instead of exposing it as a real tree) — this track's own `verbatim_sites` probe exists specifically to catch exactly that kind of shortcut. A new direct unit test (`no_blank_before_suppresses_exactly_the_next_statements_own_blank`) pins the mechanism against a minimal repro of the real shape. This field is not yet consumed by any `bynk-emit` call site — that begins with #1486's own implementation, next.
---
