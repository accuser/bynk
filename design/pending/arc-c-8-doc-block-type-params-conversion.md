---
level: minor
changelog: Arc C, slice 8 — `emit_doc_block`/`ts_type_params` build real `bynk_ts` nodes internally, closing step (1) of the design pass's own `emit.rs` decomposition order and correcting its 13-function list to the real 2 (9 were never TS-emission code; the ICU cluster is real but deferred to its own future step)
---
