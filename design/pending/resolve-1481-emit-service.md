---
level: patch
changelog: Resolves #1481 — `emit_service` no longer hand-rolls its own `Raw`-body print-time-offset recovery by string-matching the printed text's own tail (the "suppress rather than mis-record" degrade-to-no-merge #1360 finding 3 established); `emit_service` now returns one `bynk_ts::TsStmt` (was `out: &mut String` plus `source_map: Option<&RefCell<SourceMapBuilder>>`) — the whole `export const {name} = { ... };` object literal, built into a local buffer via `bynk_ts::print_object_entry_and_merge` (#1477's shared `TsObjectEntry::Method` merge helper, replacing the per-handler hand-rolled `.merge(` call this issue named directly) once per handler, carrying an object-wide `nested_map` that combines every handler's own `body_smb`, rebased from `body_out`-relative to `raw_body`-relative via `shift_checkpoints` (the same rebase already used for the subscriber-filter/schema-dispatch prologues) before being set on the handler's `Raw` body statement. Its one call site in `emit_project()` merges this single combined map into the real module map via `print_stmt_and_merge`, the same composition `emit_provider` (#1480) already established. Each handler body stays one opaque, correctly-source-mapped `Raw` node — no further decomposition (ADR 0391). `emit_class_method_and_merge_source_map`/`RawBody` are left untouched — still used by `emit_agent`'s own not-yet-converted call site (#1482). Verified: `cargo test -p bynk-emit` (187/187), `cargo test -p bynkc --test source_map --test source_map_bodies` (14/14, including `service_handler_body_keeps_its_own_statement_lines_inside_the_events_iife`, `service_handler_body_maps_per_statement`, and `service_handler_with_subscriber_and_schema_prologue_maps_per_statement` — the tests that directly exercise this code path), and the full `cargo test --workspace` (all green, byte-identical fixture output via the `tsc_verify` corpus). `ts_writes` (gated probe) moves 827 → 826.
---
