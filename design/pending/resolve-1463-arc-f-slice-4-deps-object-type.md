---
level: patch
changelog: Resolves #1463 (Arc F, slice 4) — `emit.rs`'s deps-object-type builders (`cap_ref_ty`/`build_deps_object_ty_with_surface`/`workers_env_ty`/`surface_ty`) now build real `bynk_ts::TsType` nodes instead of hand-joined strings, closing the opaque `TsType::named(deps_ty)` wrap `#1361` deliberately deferred and `emit_context_deps_interface`'s own `surface` field residual (`#1453`). `append_deps_field` widens a real `Vec<TsTypeMember>` (`Vec::push`) instead of string-splicing — review of #1469 found the old string splice was a latent hazard, not just untidy, and tightened the helper's own signature to make the panic path it carried impossible by construction. One field (the actor-sum `who` union) stays pre-rendered opaque text — its comma-separated tagged-object shape doesn't match `TsType::Object`'s own semicolon convention, a real fixture byte-diff caught this, not assumed; `surface_ty`'s own `ReturnType<T>` narrowed one level further per the same review, leaving only its `typeof X` operand opaque. Zero emitted-TypeScript change; `ts_writes` drops from 875 to 860 as a side effect (most of these functions' own internal `format!` calls for TS syntax are gone, not just the `TsParam`-level wrap).
---
