---
level: patch
changelog: Arc F slice 2 repoints `bynk-emit`'s `plan_agent_given_deps`/`native_platforms_of_context` at the tree-native `instantiate_provider_ts_expr`, deleting the now-uncalled `String`-returning `instantiate_provider_expr` — zero emitted-output change.
---
