---
level: patch
changelog: Resolves #1482 — `emit_agent` now returns a real `Vec<bynk_ts::TsStmt>` (was `out: &mut String` plus `source_map: Option<&RefCell<SourceMapBuilder>>`). Per #1462's own end-to-end audit of `emit_agent`'s 99 `ts_writes` sites: the 66 already-argued print-and-splice sites (state interface, registry, zero factory, rehydration gate, construction factory) now push their already-real top-level nodes directly into the returned `Vec` instead of being printed through the removed `out` parameter — no internal construction logic changed. Two sites stay genuinely opaque, each captured as its own `TsStmt::raw`, mirroring `emit_provider`'s Decision-C precedent (#1480): the DO class (`export class {Name} { ... }`, emit_agent's own instance of Decision C, cited in place since #1371) is built into a local buffer/map pair — every internal print-and-splice site inside it (`loadState`/`commitState`/the per-handler loop via `emit_class_method_and_merge_source_map`/the WS-hosted DO methods via `emit_ws_do_method`/`fetch`) is unchanged, just now merging into that local map instead of the function's former external parameters — and wrapped as one `TsStmt::raw` carrying a class-wide `nested_map`. The history-driver (`__bynkDriveHistory_*`, gated on `ctx.history_target_agents`) is #1386's own standing permanent-exclusion decision, reconfirmed by #1462's own correction (test-support only, stripped from deploy, thinnest fixture coverage in the track, its own per-handler discriminated-union algebra deferred to `any` by design) — genuinely raw hand-templated text with zero `bynk_ts`/`LowerCtx` involvement, captured as a second, separate `TsStmt::raw` with no `nested_map` (nothing to merge), not decomposed further. Both wraps use `TsStmt::raw`, not `TsStmt::verbatim`/`VerbatimOrigin` — per `TsStmtKind::Raw`'s own doc, `Verbatim` is reserved for temporary, not-yet-converted residue the `verbatim_sites`/`verbatim_origins` probes track toward zero, and using it for genuinely permanent content (`lower.rs`'s own exclusion, `emit_provider`'s own class wrapper, and now these two) would misrepresent it as unfinished work. `emit_agent`'s one call site in `emitter.rs` now loops the returned `Vec` through `extend_printed_and_merged` (already established by #1480 for `emit_free_fn`), the same composition `emit_service`/`emit_provider` already use. Verified: `cargo test -p bynk-emit` (187/187) and the full `cargo test --workspace` (all green, byte-identical fixture output via the `tsc_verify` corpus — both full-corpus runs, ~180s each). `ts_writes` (gated probe) moves 826 → 822 — the four retired `writeln!(out).unwrap()` blank-line calls for the state interface/zero factory/rehydration gate/construction factory, now plain `TsStmt::blank` pushes instead.
---
