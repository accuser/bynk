---
level: patch
changelog: "`bynk new` preserves a hand-written `.gitignore` instead of overwriting it — a target holding only `.git/` and a user `.gitignore` scaffolds, and the template `.gitignore` is written only when none is present (#737)."
---
