---
level: patch
changelog: Resolves #1480 — `emit_free_fn` and `emit_provider` no longer hand-roll their own `Raw`-body print-time-offset recovery by exact arithmetic (guarded by a `debug_assert!`); #1477's `bynk_ts::print_stmt_and_merge`/`print_class_method_and_merge` (via the new `TsStmt::nested_map` field) now recover that offset themselves. `emit_free_fn` returns `Vec<bynk_ts::TsStmt>` (an optional doc comment, the function decl with its own body's `nested_map` set, a trailing blank) instead of writing into `out: &mut String`; both call sites (`emit()`/`emit_project()`) print through the new `extend_printed`/`extend_printed_and_merged` helpers in `emitter.rs`. `emit_provider` returns one `bynk_ts::TsStmt` — the whole class-wrapper text (Decision C, unchanged: still one hand-written, permanently-opaque `TsStmt::raw` construction per provider declaration, not decomposed further — a real `TsDecl::Class` would need every method's own body captured into a local buffer for `Raw`-embedding anyway, and this class's own spacing convention already disagrees with `TsDecl::Class`'s established one) — built into a local buffer, carrying a class-wide `nested_map` that combines every op method's own `body_smb` (each merged in via `bynk_ts::print_class_method_and_merge` at its own real offset within that local buffer); its one call site in `emit_project()` merges this single combined map into the real module map via `print_stmt_and_merge`. `emit_class_method_and_merge_source_map`/`RawBody` (the hand-rolled recovery helper #1477 was meant to retire) are left untouched — still used by `emit_service`/`emit_agent`'s own not-yet-converted call sites (#1481/#1482). Verified: `cargo test -p bynk-emit` (187/187), `cargo test -p bynkc --test source_map --test source_map_bodies` (14/14, including `contract_guarded_body_keeps_its_own_statement_lines` and `provider_op_body_keeps_its_own_statement_lines` — the two tests that directly exercise these code paths), and the full `cargo test -p bynkc` fixture-diff + e2e suite. `ts_writes` (gated probe) moves 829 → 827 — both from `emit_free_fn`'s own retired offset-recovery code: its `debug_assert!` message built via `format!`, and its final `writeln!(out)` trailing-blank-line push, now a plain `TsStmt::blank` in the returned `Vec` instead.
---
