---
level: minor
changelog: "Arc C, slice 4: workers_entry.rs's emit_worker_entry and its four helpers (emit_scheduled_handler/emit_queue_handler/emit_http_route_dispatch/emit_call_handler_dispatch) construct and return a real bynk_ts::TsProgram instead of writeln!-ing a String -- bynk-emit/src/project.rs's own index.ts construction site drops its Verbatim/NotYetConverted wrap. Extends the bynk-ts node algebra with the four shapes Decision A named (TsStmt::Switch/TsSwitchCase -- the first genuinely new statement-grouping construct in the tree; TsDecl::ExportDefault for a bare-expression default export; TsExpr::Conditional, with proactive parenthesisation-rule coverage in all three existing rule sites; TsType::Object's Vec<(String, TsType, bool)> replaced by a TsTypeMember enum with readonly/method-signature members) plus real gaps found during implementation beyond that list: TsDecl::ReExport (export { A, B } from \"spec\";), TsStmtKind::Blank (a bare blank line usable at any nesting depth), TsLit::Bool, TsExpr::Paren (an explicit, printer-preserved parenthesisation independent of the wrapped expression's own precedence), TsTypeMember::Index (a TypeScript index signature, textually distinct from the semantically-equivalent Record<K,V>), TsBinaryOp::GreaterThan, TsStmt::If's own else_branch, and TsStmtKind::InlineBlock (a braced, multi-statement block on one generated line, plus a render_block_body special case for a TryCatch body that is one). A further correction found only by the zero-diff fixture check: the As arm's own automatic \"always parenthesize a nested Await\" rule (added by P7.8) was actually just one file's own historical text, not a grammar requirement -- removed, replaced by explicit TsExpr::Paren wraps at the three real sites that want it (two in this file, one in events_fanout.rs, since even within workers_entry.rs itself the two real await-as sites disagree with each other). render_binary_operand also gained a same-operator-chain-flattening refinement for non-nullish operators (a real 3-term || chain needs no parens around its own nested pair). deserialise_call/serialise_call/brand_assertion stay String-returning per Decision B, carried as opaque TsExpr::Ident text, the same precedent workers.rs's own conversion (#1321) already established for the same two functions. Zero output change: bless_positive_fixtures/positive_fixtures (all 116 expected/workers/*/index.ts byte-golden fixtures) and tsc_verify's full strict-tsc corpus pass unchanged. verbatim_sites 9 -> 8 (this slice's own one call site); ts_writes 1462 -> 1305 (workers_entry.rs's own 191 writeln!/write!/format! calls gone); ts_any unchanged at 30 (no any construction in this file); ast_importers stays at 5, exactly as the accepted proposal's own Framing predicted (this file's input-side AST walk is unchanged, only its output representation converted). No isolated direct fixture test was added inside workers_entry.rs itself, the same precedent workers.rs's own conversion (#1321) already set for the identical reason -- emit_worker_entry's own signature depends on whole-project, checker-derived UnitTable data with no existing precedent anywhere in bynk-emit for hand-constructing it in a crate-local unit test; the full positive_fixtures/tsc_verify suite already exercises the real, converted function against all 116 real fixtures byte-for-byte under real tsc --strict. (#1323)"
---
