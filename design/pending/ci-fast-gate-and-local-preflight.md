---
level: patch
changelog: "`cargo xtask ci` runs the CI gates locally (`--fast` for the two that need no compile-and-link, also run by an opt-in `.githooks/pre-push`), and CI reports a `CI fast gates` check ahead of `CI green`"
---
