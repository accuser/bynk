---
level: patch
changelog: Track slice T0.3: `[workspace.lints.clippy] wildcard_enum_match_arm = "warn"` recorded in the root manifest, closing the `workspace_lints` probe's `absent` reading; the 296-violation inventory is recorded on the spine (#996), not yet enforced per-crate
---
