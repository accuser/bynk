---
level: minor
changelog: Arc C, slice 23 — `emit_agent`'s per-handler methods convert to real `bynk_ts::TsClassMethod` fragments, closing the grounding pass's own second predicted algebra gap (`TsClassMethod.doc`) and extending `emit_service`'s own two-level offset pattern to a genuinely new third wrapper dimension (the implicit-commit closure, which can combine with the events-IIFE). The largest sub-slice of step (9)'s own decomposition, per the grounding pass's own prediction, confirmed accurate
---
