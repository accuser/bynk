---
level: patch
changelog: A `?`'s propagating early return nested inside a short-circuited `&&`/`||`/`implies` right operand now correctly exits the enclosing function instead of only the operator's internal wrapper
---
