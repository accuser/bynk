---
level: patch
changelog: P6.14 (design/tracks/the-ir.md §6, #1174) — bynk-emit::ir gains IrItem::Provider, the sibling assembly to P6.12's IrItem::Capability, plus ProviderBody, the type the reference's own body: ProviderBody // Bynk ops | External(module) comment names but never defines as a real Rust enum. #1174's own grounding pass found this buildable this slice, unlike its sibling Actor (#1172, settled as a non-build decision): ProviderDecl::external already carries the exact Bynk/External dispatch the reference sketch needs, and a ProviderOp's own body is a real, fully type-checked handler body (check_provider_decls pushes every op through checker::check_handler_body with the provider's own given as its capability scope, no self/state/store binding at all), so lower_provider_op_ir mirrors lower_fn_body_ir's ADR 0334 panic-on-miss rather than lower_op_sig_ir's lenient Ty::Unit fallback. ProviderBody::External deliberately carries no module field despite the reference's own parenthetical: that name lives on the adapter's own binding clause, resolved only by the whole-project pass into adapter_bindings, data no other lower_X_item_ir function in this module reaches either, and R8.1's own emission table already gives Provider{External} "nothing — the binding module supplies it", so even phase 7's own per-item printer would never consume it. No bynk-emit emission-behaviour change — every existing e2e/emission fixture passes unmodified, and still no consumer of bynk-emit::ir
---
