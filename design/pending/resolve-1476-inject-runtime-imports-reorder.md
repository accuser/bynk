---
level: patch
changelog: Resolves #1476 — `emit_project`'s runtime-import line is now built once, after the per-item body-emission loop populates `ctx.runtime_use`, with `bytes()`/`icu()` folded directly into `write_header`'s own construction instead of spliced in afterward by `inject_runtime_imports`. `write_header`'s own call moves to the end of `emit_project`; its output is prepended, and the module's `SourceMapBuilder` checkpoints (recorded against the item-only buffer) are shifted by the header's own final length first — the same `shift_checkpoints` mechanism `emit_service`'s own prologue insertion already uses. `inject_runtime_imports`/`missing_bindings` are **not** deleted (a real gap in the issue's own original grounding, found during implementation): `project/tests_emit.rs` still calls them directly for `emit_test_module`/`emit_integration_module`'s own identical post-print splice — filed as its own follow-up, #1489. Byte-identical output verified across the full fixture corpus (`tsc_verify`, `bless_positive_fixtures`/`positive_fixtures`, the full `bynkc` e2e suite).
---
