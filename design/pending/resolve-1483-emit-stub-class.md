---
level: patch
changelog: Resolves #1483 — `emit_stub_class` (`bynk-emit/src/project/tests_emit.rs`) now returns one real `bynk_ts::TsStmt` instead of a plain `String`. The class wrapper (header/fields) stays hand-written text — Decision C, the same `emit_provider` precedent (#1480) this function's own in-place comment already cited — wrapped as one `TsStmt::raw` with no `nested_map`: this function has no `LowerCtx`/source-map involvement anywhere in its own construction (`lower_stub_value_block`/`emit_stub_rhs` build already-formed text, not through a body-local `SourceMapBuilder`), unlike `emit_provider`/`emit_service`/`emit_agent`'s own per-handler bodies, so there is nothing to merge. Each method still prints as its own real `TsClassMethod` fragment via `bynk_ts::print_class_method`, spliced directly into the wrapper exactly as before — no internal construction logic changed. The one call site (`emit_test_module`) now prints the returned node through `bynk_ts::print_stmt` instead of pushing the raw `String` directly, byte-identical output. Verified: `cargo test -p bynk-emit` (187/187) and the full `cargo test --workspace` (all green, byte-identical fixture output via the `tsc_verify` corpus — both full-corpus runs, ~180s each). `ts_writes` (gated probe) unchanged — no `write!`/`writeln!`/`format!` call site was retired, only the function's own return type and its one call site's plumbing.
---
