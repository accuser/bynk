---
level: patch
changelog: An inlined boundary codec, or a `Json.decode[T]`, reaching a Worker's `compose.ts` or a test-scaffold module now imports the runtime helpers and types it names, instead of emitting TypeScript that references an unimported name
---
