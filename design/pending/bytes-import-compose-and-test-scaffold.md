---
level: patch
changelog: A `Bytes` or other inlined boundary codec reaching a Worker's `compose.ts` or a test-scaffold module now imports the runtime helpers it calls, instead of emitting TypeScript that references an unimported name
---
