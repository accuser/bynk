---
level: minor
changelog: The eight-phase compiler trajectory to the greenfield architecture (`bynk-greenfield-compiler.md`) is complete — every construct that compiled at v0.245.0 still compiles, rebuilt on the new architecture with no dropped capability.
---
<!-- Trivial retouch to re-fire the stamp workflow's push trigger: the prior
     merge (#1598) landed this file, but the stamp run never dispatched
     (a GitHub-side delivery gap during a rocky infra window that also hit
     the npm registry). This edit generates a fresh push matching the
     workflow's path filter with no content change to the frontmatter. -->

