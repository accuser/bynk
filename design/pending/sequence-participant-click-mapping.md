---
level: patch
changelog: The sequence-diagram view links each participant's click-to-code to its own box — Mermaid 11 emits several `.actor` nodes per participant, so the previous `.actor` index zip mislinked (or failed to link) participant boxes
---
