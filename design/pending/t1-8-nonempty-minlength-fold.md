---
level: minor
changelog: "`NonEmpty` now canonicalises as `MinLength(1)` (R12.2's Length domain), so a boundary type spelled either way hashes and structurally matches identically — coordinated redeploy: rebuild both sides of any already-deployed pair sharing a `NonEmpty`-refined boundary type"
---
