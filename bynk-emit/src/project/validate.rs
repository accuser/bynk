//! Empty as of P5.3 (`design/tracks/semantics-in-the-checker.md` §6):
//! `check_platform_lock` (this module's last occupant) relocated to
//! `bynk_check::project_model::phase_platform_lock`, following
//! `check_messages_bundles`/`check_locale_bundle_ambiguity` (P5.0),
//! `check_event_subscriptions` (P5.1), and `check_function_type_boundaries`
//! (P5.2) out of this file. Deletion (this module declaration along with it)
//! is P5.5's job, per §5's completion criterion — left in place, empty, until
//! then.
