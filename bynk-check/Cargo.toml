[package]
name = "bynk-check"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Bynk's semantic-analysis layer: name resolution, type checking, the kernel-method and builtin registries, first-party sources, actors, and the captured index/hints tables — the layer between bynk-syntax and the emitter."
keywords = ["compiler", "dsl", "type-checker", "language"]
categories = ["compilers"]

[lib]
path = "src/lib.rs"

[dependencies]
# The syntax foundation this layer analyses.
bynk-syntax.workspace = true
# Discovery, the unit graph, and manifest/schema-registry document shapes —
# P4.1 (#1115): the new project-level analysis entry point discovers via this
# crate and resolves/checks via this crate's own already-local modules.
bynk-project.workspace = true
# Refinement-predicate validation in the checker.
regress.workspace = true

[dev-dependencies]
# P4.1 (#1115): the differential fixture compares this crate's new analysis
# entry point against bynk-emit's `analyse_project_with`. A dev-dependency
# only (excluded from library builds), so this doesn't invert the real
# bynk-emit -> bynk-check dependency edge.
# P4.1-only: remove alongside tests/differential_analysis.rs once phase 5
# deletes `run_checks`'s `Mode::Analyse` arm and the fixture self-deletes.
bynk-emit.workspace = true
