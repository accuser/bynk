context demo.agt

agent Sessions {
	key id: String

	store live: Cache[String, Int] @ttl(5.minutes)
	store items: Map[String, Int] @indexed(by: id) @bounded(10000)

	on call read() -> Effect[Int] { Effect.pure(0) }
}
