context app

uses shared.values

-- Review finding #9, same bug as fixture 951 but for `T.unsafe(value)`.
agent Box {
  key id: String
  store hits: Cell[Int] = 0

  on call make(s: String) -> Effect[Token] {
    Effect.pure(Token.unsafe(s))
  }
}
