context commerce.notifications

consumes commerce.order
consumes bynk

service OnPayment from Events(PaymentConfirmed) {
  on event(e: PaymentConfirmed, env: EventEnvelope, extra: String) -> Effect[()] {
    ()
  }
}
