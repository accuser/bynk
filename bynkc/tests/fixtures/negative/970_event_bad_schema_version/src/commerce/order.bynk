context commerce.order

exports transparent { PaymentConfirmed }

event PaymentConfirmed @schema(0) = {
  orderId: String,
}

service markPaid {
  on call(orderId: String) -> Effect[()] {
    Effect.pure(())
  }
}
