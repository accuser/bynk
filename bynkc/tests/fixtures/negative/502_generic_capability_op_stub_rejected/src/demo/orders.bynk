context demo.orders

consumes idempotency { Idempotency }

service reserve {
  on call() -> Effect[Option[Int]] given Idempotency {
    let cached <- Idempotency.dedup[Int]("order-key-1")
    cached
  }
}
