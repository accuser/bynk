context commerce.order

exports transparent { PaymentConfirmed }

event PaymentConfirmed = {
  orderId: String,
}

service OnPayment from Events(PaymentConfirmed) via schema(5..2) {
  on event(e: PaymentConfirmed) -> Effect[()] {
    Effect.pure(())
  }
}
