context svc

service api from http {
  on GET("/orders/:id") (id: String) -> Effect[HttpResult[String]] by v: Visitor {
    Ok("first")
  }

  on GET("/orders/:id") (id: String) -> Effect[HttpResult[String]] by v: Visitor {
    Ok("second")
  }
}
