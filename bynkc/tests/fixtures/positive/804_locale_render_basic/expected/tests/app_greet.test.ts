// Generated by bynkc — do not edit by hand.
// test target: app.greet

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "../runtime.js";
import * as app_greet from "./../app/greet.js";
import * as bynk from "./../bynk.js";
import * as bynk_locale from "./../bynk/locale.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function makeTestDeps() {
  return {  };
}

// case tier: unit
async function test_no_params_renders_just_the_code() {
  try {
    const deps = makeTestDeps();
    const { greeting, greetingWithCountAndName, greetingWithName } = app_greet as any;
    const { LocaleTag, Message, MessageArg, message, render, renderArg, withMoment, withNum, withText, withWhole } = bynk_locale as any;
    const { FetchError, Method, Request, Response, Uuid } = bynk as any;
    const g = await greeting.call(deps);
    if (!(g === "hello")) { throw __bynkExpectFailure("tests/greet.test.bynk:10:12", 390, 402, "expect g == \"hello\"\n  expected: g == \"hello\"\n  actual:   " + __bynkShow((g)) + " == " + __bynkShow(("hello"))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_one_param_renders_code_plus_the_substitution() {
  try {
    const deps = makeTestDeps();
    const { greeting, greetingWithCountAndName, greetingWithName } = app_greet as any;
    const { LocaleTag, Message, MessageArg, message, render, renderArg, withMoment, withNum, withText, withWhole } = bynk_locale as any;
    const { FetchError, Method, Request, Response, Uuid } = bynk as any;
    const g = await greetingWithName.call("Ada", deps);
    if (!(g === "greeting {name=Ada}")) { throw __bynkExpectFailure("tests/greet.test.bynk:15:12", 517, 543, "expect g == \"greeting {name=Ada}\"\n  expected: g == \"greeting {name=Ada}\"\n  actual:   " + __bynkShow((g)) + " == " + __bynkShow(("greeting {name=Ada}"))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_multiple_params_render_sorted_by_key__not_call_order() {
  try {
    const deps = makeTestDeps();
    const { greeting, greetingWithCountAndName, greetingWithName } = app_greet as any;
    const { LocaleTag, Message, MessageArg, message, render, renderArg, withMoment, withNum, withText, withWhole } = bynk_locale as any;
    const { FetchError, Method, Request, Response, Uuid } = bynk as any;
    const g = await greetingWithCountAndName.call("Ada", 3, deps);
    if (!(g === "greeting {count=3, name=Ada}")) { throw __bynkExpectFailure("tests/greet.test.bynk:20:12", 677, 712, "expect g == \"greeting {count=3, name=Ada}\"\n  expected: g == \"greeting {count=3, name=Ada}\"\n  actual:   " + __bynkShow((g)) + " == " + __bynkShow(("greeting {count=3, name=Ada}"))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("no params renders just the code")) results.push({ name: "no params renders just the code", ...(await test_no_params_renders_just_the_code()) });
  if (want("one param renders code plus the substitution")) results.push({ name: "one param renders code plus the substitution", ...(await test_one_param_renders_code_plus_the_substitution()) });
  if (want("multiple params render sorted by key, not call order")) results.push({ name: "multiple params render sorted by key, not call order", ...(await test_multiple_params_render_sorted_by_key__not_call_order()) });
  return results;
}
