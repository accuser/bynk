// Generated by bynkc — do not edit by hand.
// test target: blobs

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, __bynkBytesEqual, __bynkBytesToBase64, __bynkBytesFromBase64, __bynkBytesDecodeUtf8 } from "../runtime.js";
import * as blobs from "./../blobs.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function makeTestDeps() {
  return {  };
}

// case tier: unit
async function test_decodes_base64() {
  try {
    const deps = {};
    const { size } = blobs as any;
    void (((__d) => {
        switch (__d.tag) {
          case "Some": {
            const b = __d.value;
            return __bynkExpect((size(b) === 2), "tests/blobs.test.bynk:8:25", 353, 365, "expect size(b) == 2");
          }
          case "None": {
            return __bynkExpect((false), "tests/blobs.test.bynk:9:22", 387, 392, "expect false");
          }
        }
        throw new Error("non-exhaustive match");
      })(__bynkBytesFromBase64("aGk=")));
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_compares_by_content() {
  try {
    const deps = {};
    const { size } = blobs as any;
    if (!(__bynkBytesEqual(new TextEncoder().encode("hi"), new TextEncoder().encode("hi")))) { throw __bynkExpectFailure("tests/blobs.test.bynk:14:12", 446, 490, "expect Bytes.fromUtf8(\"hi\") == Bytes.fromUtf8(\"hi\")\n  expected: Bytes.fromUtf8(\"hi\") == Bytes.fromUtf8(\"hi\")\n  actual:   " + __bynkShow((new TextEncoder().encode("hi"))) + " == " + __bynkShow((new TextEncoder().encode("hi")))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("decodes base64")) results.push({ name: "decodes base64", ...(await test_decodes_base64()) });
  if (want("compares by content")) results.push({ name: "compares by content", ...(await test_compares_by_content()) });
  return results;
}
