// Generated by bynkc — do not edit by hand.
// commons probe

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "./runtime.js";

export type Flag =
    { readonly tag: "On" }
  | { readonly tag: "Off" };

export const Flag = {
  On: { tag: "On" } as Flag,
  Off: { tag: "Off" } as Flag,
};

export function risky(): Result<Flag, string> {
  return Ok(Flag.On);
}

export function f(): Result<number, string> {
  const __r0 = risky();
  if (__r0.tag === "Err") return __r0;
  const x = ((__d) => {
    switch (__d.tag) {
      case "On": {
        return 1;
      }
      case "Off": {
        return 0;
      }
    }
    throw new Error("non-exhaustive match");
  })(__r0.value);
  return Ok(x);
}

