// Generated by bynkc — do not edit by hand.
// commons demo

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonError, type JsonValue, type BoundaryError } from "./runtime.js";

export interface Box {
  readonly value: string;
}

export const Box = {
  encode(self: Box, note: string): string {
    return self.value.concat(note);
  },
};

export function shadowed(Json: Box, note: string): string {
  return Box.encode(Json, note);
}

export function genuine(o: Box): string {
  return JSON.stringify(serialise_Box(o));
}

export function serialise_Box(value: Box): JsonValue {
  return {
    value: value.value as JsonValue,
  };
}

export function deserialise_Box(json: JsonValue, path: string = "$"): Result<Box, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (typeof obj["value"] !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}.value`, expected: "string", actual: typeof obj["value"] });
  }
  const __value = obj["value"];
  return Ok({ value: __value } as Box);
}


