// Generated by bynkc — do not edit by hand.
// test target: demo.stubs

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "../runtime.js";
import * as demo_stubs from "./../demo/stubs.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function __bynkDeepEqual(a: unknown, b: unknown): boolean {
  const s = (v: unknown) => JSON.stringify(v, (_k, val) => typeof val === "bigint" ? "__bigint__" + String(val) : val);
  try { return s(a) === s(b); } catch { return a === b; }
}

class __Stub_Vault {
  __seq_0 = 0;
  __seq_1 = 0;
  async open(): Promise<number> {
    if (true) {
      throw new Error("bynk: injected capability fault (stubs … fails)");
    }
    throw new Error("bynk: no stub clause matched for Vault.open");
  }
  async peek(): Promise<number> {
    if (true) {
      const __k = this.__seq_0;
      if (this.__seq_0 < 1) this.__seq_0++;
      switch (__k) {
        case 0: {
          return 1;
        }
        default: {
          throw new Error("bynk: injected capability fault (stubs … fails)");
        }
      }
    }
    if (true) {
      const __k = this.__seq_1;
      switch (__k) {
        default: {
          return 42;
        }
      }
    }
    throw new Error("bynk: no stub clause matched for Vault.peek");
  }
}

function makeTestDeps() {
  return { Vault: new __Stub_Vault() };
}

// case tier: unit
async function test_a_direct_fails_clause() {
  try {
    const deps = makeTestDeps();
    const { box } = demo_stubs;
    const r = await box.call(deps);
    if (!(true)) { throw __bynkExpectFailure("tests/stubs.test.bynk:11:12", 431, 435, "expect true"); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_returns_each_ending_in_fails() {
  try {
    const deps = makeTestDeps();
    const { box } = demo_stubs;
    const r = await box.call(deps);
    if (!(true)) { throw __bynkExpectFailure("tests/stubs.test.bynk:17:12", 562, 566, "expect true"); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_a_single_outcome_returns_each() {
  try {
    const deps = makeTestDeps();
    const { box } = demo_stubs;
    const r = await box.call(deps);
    if (!(true)) { throw __bynkExpectFailure("tests/stubs.test.bynk:23:12", 688, 692, "expect true"); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("a direct fails clause")) results.push({ name: "a direct fails clause", ...(await test_a_direct_fails_clause()) });
  if (want("returns each ending in fails")) results.push({ name: "returns each ending in fails", ...(await test_returns_each_ending_in_fails()) });
  if (want("a single-outcome returns each")) results.push({ name: "a single-outcome returns each", ...(await test_a_single_outcome_returns_each()) });
  return results;
}
