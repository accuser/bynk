// Generated by bynkc — do not edit by hand.
// Worker entry point for context `secured`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError, matchPath, httpResultToResponse, headResponse, notModifiedIfMatch, type SecurityPolicy, applySecurityHeaders } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    const __security_admin: SecurityPolicy = { nosniff: false, hstsMaxAgeSecs: null };
    const __security_health: SecurityPolicy = { nosniff: true, hstsMaxAgeSecs: null };
    const __security_store: SecurityPolicy = { nosniff: true, hstsMaxAgeSecs: 15552000 };
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      {
        if ((method === "GET" || method === "HEAD") && path === "/admin/stats") {
          const result = await surface.http_GET_admin_stats();
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (v: any) => v as JsonValue, { weakEtag: true }), request), __security_admin);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/health") {
          const result = await surface.http_GET_health();
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (v: any) => v as JsonValue, { weakEtag: true }), request), __security_health);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        const __m = matchPath("/products/:id", path);
        if ((method === "GET" || method === "HEAD") && __m) {
          const __raw_id = __m.params["id"];
          const id = __raw_id;
          const result = await surface.http_GET_products_Param_id(id);
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (v: any) => v as JsonValue, { weakEtag: true }), request), __security_store);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      if (path === "/admin/stats") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_admin);
      }
      if (path === "/health") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_health);
      }
      if (matchPath("/products/:id", path) !== null) {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_store);
      }
      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
