// Generated by bynkc — do not edit by hand.
// test target: app.bundle

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "../runtime.js";
import * as app_bundle from "./../app/bundle.js";
import * as bynk_locale from "./../bynk/locale.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function makeTestDeps() {
  return {  };
}

// case tier: unit
async function test_bare_date_renders_a_non_empty_string_containing_the_year() {
  try {
    const deps = {};
    const { render } = app_bundle as any;
    const { LocaleTag, Message, MessageArg, message, renderArg, withMoment, withNum, withText, withWhole } = bynk_locale as any;
    const msg = withMoment(message("published"), "d", 1592222400000);
    if (!(render(("en" as any), msg).includes("2020"))) { throw __bynkExpectFailure("tests/bundle.test.bynk:12:12", 585, 619, "expect render(\"en\", msg).contains(\"2020\")"); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_full_style_is_longer_than_short_style__more_detail__not_less_() {
  try {
    const deps = {};
    const { render } = app_bundle as any;
    const { LocaleTag, Message, MessageArg, message, renderArg, withMoment, withNum, withText, withWhole } = bynk_locale as any;
    const short = render(("en" as any), withMoment(message("published.short"), "d", 1592222400000));
    const full = render(("en" as any), withMoment(message("published.full"), "d", 1592222400000));
    if (!((full).length > (short).length)) { throw __bynkExpectFailure("tests/bundle.test.bynk:18:12", 935, 965, "expect full.length() > short.length()\n  expected: full.length() > short.length()\n  actual:   " + __bynkShow(((full).length)) + " > " + __bynkShow(((short).length))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_missing_param_falls_back_to_the_literal_placeholder_text() {
  try {
    const deps = {};
    const { render } = app_bundle as any;
    const { LocaleTag, Message, MessageArg, message, renderArg, withMoment, withNum, withText, withWhole } = bynk_locale as any;
    const msg = message("published");
    if (!(render(("en" as any), msg) === "Published {d}")) { throw __bynkExpectFailure("tests/bundle.test.bynk:23:12", 1085, 1121, "expect render(\"en\", msg) == \"Published {d}\"\n  expected: render(\"en\", msg) == \"Published {d}\"\n  actual:   " + __bynkShow((render(("en" as any), msg))) + " == " + __bynkShow(("Published {d}"))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("bare date renders a non-empty string containing the year")) results.push({ name: "bare date renders a non-empty string containing the year", ...(await test_bare_date_renders_a_non_empty_string_containing_the_year()) });
  if (want("full style is longer than short style (more detail, not less)")) results.push({ name: "full style is longer than short style (more detail, not less)", ...(await test_full_style_is_longer_than_short_style__more_detail__not_less_()) });
  if (want("missing param falls back to the literal placeholder text")) results.push({ name: "missing param falls back to the literal placeholder text", ...(await test_missing_param_falls_back_to_the_literal_placeholder_text()) });
  return results;
}
