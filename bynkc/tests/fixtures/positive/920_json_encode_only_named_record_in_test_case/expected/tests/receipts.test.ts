// Generated by bynkc — do not edit by hand.
// test target: receipts

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type BoundaryError, type JsonValue, type JsonError } from "../runtime.js";
import * as receipts from "./../receipts.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function makeTestDeps() {
  return {  };
}

// case tier: unit
async function test_encodes_a_named_type() {
  try {
    const deps = {};
    const { Receipt } = receipts;
    type Receipt = receipts.Receipt;
    if (!(JSON.stringify(serialise_Receipt({ code: "A1", total: 42 })) === "{\"code\":\"A1\",\"total\":42}")) { throw __bynkExpectFailure("tests/receipts.test.bynk:12:12", 731, 813, "expect Json.encode(Receipt { code: \"A1\", total: 42 }) == \"{\\\"code\\\":\\\"A1\\\",\\\"total\\\":42}\"\n  expected: Json.encode(Receipt { code: \"A1\", total: 42 }) == \"{\\\"code\\\":\\\"A1\\\",\\\"total\\\":42}\"\n  actual:   " + __bynkShow((JSON.stringify(serialise_Receipt({ code: "A1", total: 42 })))) + " == " + __bynkShow(("{\"code\":\"A1\",\"total\":42}"))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export function serialise_Receipt(value: receipts.Receipt): JsonValue {
  return {
    code: value.code as JsonValue,
    total: value.total as JsonValue,
  };
}

export function deserialise_Receipt(json: JsonValue, path: string = "$"): Result<receipts.Receipt, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (typeof obj["code"] !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}.code`, expected: "string", actual: typeof obj["code"] });
  }
  const __code = obj["code"];
  if (typeof obj["total"] !== "number") {
    return Err({ kind: "StructuralMismatch", path: `${path}.total`, expected: "number", actual: typeof obj["total"] });
  }
  if (!Number.isInteger(obj["total"])) {
    return Err({ kind: "StructuralMismatch", path: `${path}.total`, expected: "integer", actual: String(obj["total"]) });
  }
  const __total = obj["total"];
  return Ok({ code: __code, total: __total } as receipts.Receipt);
}


export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("encodes a named type")) results.push({ name: "encodes a named type", ...(await test_encodes_a_named_type()) });
  return results;
}
