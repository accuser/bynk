// Generated by bynkc — do not edit by hand.
// commons greet.text

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "../runtime.js";

export type Subject = string & { readonly __brand: "Subject" };

export const Subject = {
  of(value: string): Result<Subject, ValidationError> {
    if (!(value.length > 0)) {
      return Err({ field: "Subject", message: "must be non-empty", value });
    }
    if (!(value.length <= 40)) {
      return Err({ field: "Subject", message: "length must be at most 40", value });
    }
    return Ok(value as Subject);
  },
};

export function greeting(subject: Subject): string {
  return `Hello, ${String(subject)}!`;
}

