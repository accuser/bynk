// Generated by bynkc — do not edit by hand.
// commons beta

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "./runtime.js";

export type B = number & { readonly __brand: "B" };

export const B = {
  of(value: number): Result<B, ValidationError> {
    if (!Number.isInteger(value)) {
      return Err({ field: "B", message: "must be an integer", value });
    }
    if (!(value >= 0)) {
      return Err({ field: "B", message: "must be non-negative", value });
    }
    return Ok(value as B);
  },
};

export function serialise_B(value: B): JsonValue {
  return value as unknown as number;
}

export function deserialise_B(json: JsonValue, path: string = "$"): Result<B, BoundaryError> {
  if (typeof json !== "number") {
    return Err({ kind: "StructuralMismatch", path, expected: "number", actual: typeof json });
  }
  const validated = (typeof (B as unknown as { of?: (json: unknown) => Result<unknown, ValidationError> }).of === "function")
    ? (B as unknown as { of: (json: unknown) => Result<unknown, ValidationError> }).of(json)
    : (Ok(json as unknown as B) as Result<unknown, ValidationError>);
  if (validated.tag === "Err") {
    return Err({ kind: "RefinementViolation", path, violation: validated.error });
  }
  return Ok(validated.value as B);
}

