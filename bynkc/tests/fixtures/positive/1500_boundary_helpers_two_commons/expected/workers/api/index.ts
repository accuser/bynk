// Generated by bynkc — do not edit by hand.
// Worker entry point for context `api`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          case "check": {
            const __contract = request.headers.get("X-Bynk-Contract");
            if (__contract !== "14ef1b672307aadc") return new Response(JSON.stringify({ kind: "ContractMismatch", service: "check", expected: "14ef1b672307aadc", actual: __contract }), { status: 409, headers: { "content-type": "application/json" } });
            const args = await request.json() as JsonValue;
            if (typeof args !== "object" || args === null || Array.isArray(args)) return new Response(JSON.stringify({ kind: "StructuralMismatch", path: "$", expected: "object", actual: typeof args }), { status: 400, headers: { "content-type": "application/json" } });
            const argsObj = args as { [k: string]: JsonValue };
            const __r_a = handlers.deserialise_A(argsObj["a"], "$.a");
            if (__r_a.tag === "Err") return new Response(JSON.stringify(__r_a.error), { status: 400, headers: { "content-type": "application/json" } });
            const a = __r_a.value as unknown as handlers.A;
            const __r_b = handlers.deserialise_B(argsObj["b"], "$.b");
            if (__r_b.tag === "Err") return new Response(JSON.stringify(__r_b.error), { status: 400, headers: { "content-type": "application/json" } });
            const b = __r_b.value as unknown as handlers.B;
            const result = await surface.check(a, b);
            const body = result as JsonValue;
            return new Response(JSON.stringify(body), { status: 200, headers: { "content-type": "application/json" } });
          }
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
