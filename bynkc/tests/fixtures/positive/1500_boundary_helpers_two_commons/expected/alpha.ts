// Generated by bynkc — do not edit by hand.
// commons alpha

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "./runtime.js";

export type A = number & { readonly __brand: "A" };

export const A = {
  of(value: number): Result<A, ValidationError> {
    if (!Number.isInteger(value)) {
      return Err({ field: "A", message: "must be an integer", value });
    }
    if (!(value >= 0)) {
      return Err({ field: "A", message: "must be non-negative", value });
    }
    return Ok(value as A);
  },
};

export function serialise_A(value: A): JsonValue {
  return value as unknown as number;
}

export function deserialise_A(json: JsonValue, path: string = "$"): Result<A, BoundaryError> {
  if (typeof json !== "number") {
    return Err({ kind: "StructuralMismatch", path, expected: "number", actual: typeof json });
  }
  const validated = (typeof (A as unknown as { of?: (json: unknown) => Result<unknown, ValidationError> }).of === "function")
    ? (A as unknown as { of: (json: unknown) => Result<unknown, ValidationError> }).of(json)
    : (Ok(json as unknown as A) as Result<unknown, ValidationError>);
  if (validated.tag === "Err") {
    return Err({ kind: "RefinementViolation", path, violation: validated.error });
  }
  return Ok(validated.value as A);
}

