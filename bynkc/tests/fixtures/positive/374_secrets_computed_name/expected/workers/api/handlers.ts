// Generated by bynkc — do not edit by hand.
// context api

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, HttpResult, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../../runtime.js";

import * as bynk from "../../bynk.js";

export const api = {
  async http_GET_secret_Param_which(which: string, deps: { Secrets: bynk.Secrets }): Promise<HttpResult<string>> {
    const chosen = await deps.Secrets.get(which);
    const known = await deps.Secrets.get("WELL_KNOWN");
    switch (chosen.tag) {
      case "Some": {
        const s = chosen.value;
        return HttpResult.Ok(s);
      }
      case "None": {
        return ((__d) => {
    switch (__d.tag) {
      case "Some": {
        const k = __d.value;
        return HttpResult.Ok(k);
      }
      case "None": {
        return HttpResult.Ok("none");
      }
    }
    throw new Error("non-exhaustive match");
  })(known);
      }
    }
    throw new Error("non-exhaustive match");
  },
};

