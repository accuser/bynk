// Generated by bynkc — do not edit by hand.
// Worker entry point for context `api`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError, matchPath, httpResultToResponse, headResponse, notModifiedIfMatch, type SecurityPolicy, applySecurityHeaders, __bynkBytesEqual, __bynkBytesToBase64, __bynkBytesFromBase64, __bynkBytesDecodeUtf8 } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    const __security_api: SecurityPolicy = { nosniff: true, hstsMaxAgeSecs: null };
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      {
        if ((method === "GET" || method === "HEAD") && path === "/blob") {
          const result = await surface.http_GET_blob();
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (__v: any) => __bynkBytesToBase64(__v) as JsonValue, { weakEtag: true }), request), __security_api);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/ratio") {
          const result = await surface.http_GET_ratio();
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (__v: any) => ((v: number) => { if (!Number.isFinite(v)) throw new Error("non-finite Float at boundary"); return v as JsonValue; })(__v), { weakEtag: true }), request), __security_api);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      if (path === "/blob") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_api);
      }
      if (path === "/ratio") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_api);
      }
      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
