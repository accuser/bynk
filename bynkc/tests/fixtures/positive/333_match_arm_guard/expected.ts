// Generated by bynkc — do not edit by hand.
// commons test

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "./runtime.js";

export type Req =
    { readonly tag: "Get"; readonly path: string }
  | { readonly tag: "Post"; readonly body: string };

export const Req = {
  Get: (path: string): Req => ({ tag: "Get", path }),
  Post: (body: string): Req => ({ tag: "Post", body }),
};

export function classify(n: number): string {
  if (n === 0) {
    return "zero";
  }
  const x = n;
  if (x > 0) {
    return "positive";
  }
  return "negative";
}

export function route(r: Req): string {
  if (r.tag === "Get") {
    const path = r.path;
    if (path === "/api") {
      return "api";
    }
  }
  if (r.tag === "Get") {
    const path = r.path;
    return path;
  }
  if (r.tag === "Post") {
    const body = r.body;
    return body;
  }
  throw new Error("non-exhaustive match");
}

