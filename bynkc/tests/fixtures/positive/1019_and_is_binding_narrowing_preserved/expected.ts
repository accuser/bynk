// Generated by bynkc — do not edit by hand.
// commons probe

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "./runtime.js";

export type Outcome =
    { readonly tag: "Hit"; readonly score: number }
  | { readonly tag: "Miss" };

export const Outcome = {
  Hit: (score: number): Outcome => ({ tag: "Hit", score }),
  Miss: { tag: "Miss" } as Outcome,
};

export function risky(): Result<number, string> {
  return Ok(1);
}

export function check(o: Outcome): Result<number, string> {
  let __r1: boolean;
  if (o.tag === "Hit") {
    const score = o.score;
    const __r0 = risky();
    if (__r0.tag === "Err") return __r0;
    __r1 = __r0.value > 0;
  } else {
    __r1 = false;
  }
  if (o.tag === "Hit" && __r1) {
    const score = o.score;
    return Ok(score);
  } else {
    return Ok(0);
  }
}

