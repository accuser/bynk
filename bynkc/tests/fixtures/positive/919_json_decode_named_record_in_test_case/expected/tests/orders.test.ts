// Generated by bynkc — do not edit by hand.
// test target: orders

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type BoundaryError, type JsonValue, type JsonError } from "../runtime.js";
import * as orders from "./../orders.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function makeTestDeps() {
  return {  };
}

// case tier: unit
async function test_decodes_a_named_type() {
  try {
    const deps = {};
    const { Order } = orders as any;
    void (((__d) => {
        switch (__d.tag) {
          case "Ok": {
            const o = __d.value;
            return __bynkExpect((o.qty === 2), "tests/orders.test.bynk:13:23", 753, 763, "expect o.qty == 2");
          }
          case "Err": {
            return __bynkExpect((false), "tests/orders.test.bynk:14:24", 787, 792, "expect false");
          }
        }
        throw new Error("non-exhaustive match");
      })(((__s: string): Result<orders.Order, JsonError> => { let __j: JsonValue; try { __j = JSON.parse(__s) as JsonValue; } catch (__e) { return Err({ kind: "Malformed", path: "$", message: String(__e) }); } const __r = deserialise_Order(__j, "$"); if (__r.tag === "Ok") return Ok(__r.value as orders.Order); const __be = __r.error; return Err({ kind: __be.kind, path: (__be.kind === "StructuralMismatch" || __be.kind === "RefinementViolation") ? __be.path : "$", message: __be.kind === "StructuralMismatch" ? `expected ${__be.expected}, got ${String(__be.actual)}` : __be.kind === "RefinementViolation" ? __be.violation.message : __be.details }); })("{\"id\": \"a1\", \"qty\": 2}")));
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_encodes_a_named_type() {
  try {
    const deps = {};
    const { Order } = orders as any;
    if (!(JSON.stringify(serialise_Order({ id: "a1", qty: 2 })) === "{\"id\":\"a1\",\"qty\":2}")) { throw __bynkExpectFailure("tests/orders.test.bynk:19:12", 847, 917, "expect Json.encode(Order { id: \"a1\", qty: 2 }) == \"{\\\"id\\\":\\\"a1\\\",\\\"qty\\\":2}\"\n  expected: Json.encode(Order { id: \"a1\", qty: 2 }) == \"{\\\"id\\\":\\\"a1\\\",\\\"qty\\\":2}\"\n  actual:   " + __bynkShow((JSON.stringify(serialise_Order({ id: "a1", qty: 2 })))) + " == " + __bynkShow(("{\"id\":\"a1\",\"qty\":2}"))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export function serialise_Order(value: orders.Order): JsonValue {
  return {
    id: value.id as JsonValue,
    qty: value.qty as JsonValue,
  };
}

export function deserialise_Order(json: JsonValue, path: string = "$"): Result<orders.Order, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (typeof obj["id"] !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}.id`, expected: "string", actual: typeof obj["id"] });
  }
  const __id = obj["id"];
  if (typeof obj["qty"] !== "number") {
    return Err({ kind: "StructuralMismatch", path: `${path}.qty`, expected: "number", actual: typeof obj["qty"] });
  }
  if (!Number.isInteger(obj["qty"])) {
    return Err({ kind: "StructuralMismatch", path: `${path}.qty`, expected: "integer", actual: String(obj["qty"]) });
  }
  const __qty = obj["qty"];
  return Ok({ id: __id, qty: __qty } as orders.Order);
}


export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("decodes a named type")) results.push({ name: "decodes a named type", ...(await test_decodes_a_named_type()) });
  if (want("encodes a named type")) results.push({ name: "encodes a named type", ...(await test_encodes_a_named_type()) });
  return results;
}
