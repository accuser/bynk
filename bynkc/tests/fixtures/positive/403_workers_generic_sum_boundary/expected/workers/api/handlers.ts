// Generated by bynkc — do not edit by hand.
// context api

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../../runtime.js";

export type ApiResult<T> =
    { readonly tag: "Loaded"; readonly value: T }
  | { readonly tag: "Failed"; readonly message: string };

export const ApiResult = {
  Loaded: <T>(value: T): ApiResult<T> => ({ tag: "Loaded", value }),
  Failed: <T>(message: string): ApiResult<T> => ({ tag: "Failed", message }),
};

export interface User {
  readonly id: number;
  readonly name: string;
}

export const User = {
};

export const results = {
  async call(input: ApiResult<User>, deps: {}): Promise<Result<ApiResult<User>, void>> {
    return Ok(input);
  },
};

export function serialise_User(value: User): JsonValue {
  return {
    id: value.id as JsonValue,
    name: value.name as JsonValue,
  };
}

export function deserialise_User(json: JsonValue, path: string = "$"): Result<User, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (typeof obj["id"] !== "number") {
    return Err({ kind: "StructuralMismatch", path: `${path}.id`, expected: "number", actual: typeof obj["id"] });
  }
  if (!Number.isInteger(obj["id"])) {
    return Err({ kind: "StructuralMismatch", path: `${path}.id`, expected: "integer", actual: String(obj["id"]) });
  }
  const __id = obj["id"];
  if (typeof obj["name"] !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}.name`, expected: "string", actual: typeof obj["name"] });
  }
  const __name = obj["name"];
  return Ok({ id: __id, name: __name } as User);
}


export function serialise_ApiResult_User(value: ApiResult<User>): JsonValue {
  switch (value.tag) {
    case "Loaded": {
      return { kind: "Loaded", value: serialise_User((value as any).value) };
    }
    case "Failed": {
      return { kind: "Failed", message: (value as any).message as JsonValue };
    }
  }
}

export function deserialise_ApiResult_User(json: JsonValue, path: string = "$"): Result<ApiResult<User>, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const kind = obj["kind"];
  switch (kind) {
    case "Loaded": {
  const __r_value = deserialise_User(obj["value"], `${path}.value`);
  if (__r_value.tag === "Err") return __r_value;
  const __value = __r_value.value;
      return Ok({ tag: "Loaded", value: __value } as ApiResult<User>);
    }
    case "Failed": {
  if (typeof obj["message"] !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}.message`, expected: "string", actual: typeof obj["message"] });
  }
  const __message = obj["message"];
      return Ok({ tag: "Failed", message: __message } as ApiResult<User>);
    }
    default:
      return Err({ kind: "StructuralMismatch", path, expected: "sum variant kind", actual: String(kind) });
  }
}

export function serialise_Result_ApiResult_User_Unit(value: Result<ApiResult<User>, void>): JsonValue {
  if (value.tag === "Ok") return { kind: "Ok", value: serialise_ApiResult_User(value.value) };
  return { kind: "Err", error: null };
}

export function deserialise_Result_ApiResult_User_Unit(json: JsonValue, path: string = "$"): Result<Result<ApiResult<User>, void>, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (obj["kind"] === "Ok") {
  const __r_v = deserialise_ApiResult_User(obj["value"], `${path}.value`);
  if (__r_v.tag === "Err") return __r_v;
  const __v = __r_v.value;
    return Ok(Ok(__v) as Result<ApiResult<User>, void>);
  } else if (obj["kind"] === "Err") {
  const __e = undefined;
    return Ok(Err(__e) as Result<ApiResult<User>, void>);
  }
  return Err({ kind: "StructuralMismatch", path, expected: "Ok | Err", actual: String(obj["kind"]) });
}

