// Generated by bynkc — do not edit by hand.
// context commerce.notifications

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../../runtime.js";

import { type PaymentConfirmed } from "../commerce-order/handlers.js";

import * as bynk from "../../bynk.js";
import type * as commerce_order from "../commerce-order/handlers.js";

export const OnPayment = {
  async event(e: PaymentConfirmed, deps: { Logger: bynk.Logger }): Promise<void> {
    return deps.Logger.info(e.orderId);
  },
};

export function serialise_PaymentConfirmed(value: commerce_order.PaymentConfirmed): JsonValue {
  return {
    orderId: value.orderId as JsonValue,
    region: serialise_Option_Region(value.region),
  };
}

export function deserialise_PaymentConfirmed(json: JsonValue, path: string = "$"): Result<commerce_order.PaymentConfirmed, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (typeof obj["orderId"] !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}.orderId`, expected: "string", actual: typeof obj["orderId"] });
  }
  const __orderId = obj["orderId"];
  const __d_region: JsonValue = "region" in obj ? obj["region"] : { kind: "Some", value: { kind: "Domestic" } };
  const __r_region = deserialise_Option_Region(__d_region, `${path}.region`);
  if (__r_region.tag === "Err") return __r_region;
  const __region = __r_region.value;
  return Ok({ orderId: __orderId, region: __region } as commerce_order.PaymentConfirmed);
}

export function serialise_Region(value: commerce_order.Region): JsonValue {
  switch (value.tag) {
    case "Domestic":
      return { kind: "Domestic" };
    case "International":
      return { kind: "International" };
  }
}

export function deserialise_Region(json: JsonValue, path: string = "$"): Result<commerce_order.Region, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const kind = obj["kind"];
  switch (kind) {
    case "Domestic":
      return Ok({ tag: "Domestic" } as commerce_order.Region);
    case "International":
      return Ok({ tag: "International" } as commerce_order.Region);
    default:
      return Err({ kind: "StructuralMismatch", path, expected: "sum variant kind", actual: String(kind) });
  }
}


export function serialise_Option_Region(value: Option<commerce_order.Region>): JsonValue {
  if (value.tag === "Some") return { kind: "Some", value: serialise_Region(value.value) };
  return { kind: "None" };
}

export function deserialise_Option_Region(json: JsonValue, path: string = "$"): Result<Option<commerce_order.Region>, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (obj["kind"] === "Some") {
  const __r_v = deserialise_Region(obj["value"], `${path}.value`);
  if (__r_v.tag === "Err") return __r_v;
  const __v = __r_v.value;
    return Ok(Some(__v) as Option<commerce_order.Region>);
  } else if (obj["kind"] === "None") {
    return Ok(None as Option<commerce_order.Region>);
  }
  return Err({ kind: "StructuralMismatch", path, expected: "Some | None", actual: String(obj["kind"]) });
}

