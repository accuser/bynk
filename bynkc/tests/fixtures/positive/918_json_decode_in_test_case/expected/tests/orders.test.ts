// Generated by bynkc — do not edit by hand.
// test target: orders

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type BoundaryError, type JsonValue, type JsonError } from "../runtime.js";
import * as orders from "./../orders.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function makeTestDeps() {
  return {  };
}

// case tier: unit
async function test_decodes_a_base_type() {
  try {
    const deps = {};
    const { double } = orders;
    void (((__d) => {
        switch (__d.tag) {
          case "Ok": {
            const n = __d.value;
            return __bynkExpect((double(n) === 84), "tests/orders.test.bynk:17:23", 923, 938, "expect double(n) == 84");
          }
          case "Err": {
            return __bynkExpect((false), "tests/orders.test.bynk:18:24", 962, 967, "expect false");
          }
        }
        throw new Error("non-exhaustive match");
      })(((__s: string): Result<number, JsonError> => { let __j: JsonValue; try { __j = JSON.parse(__s) as JsonValue; } catch (__e) { return Err({ kind: "Malformed", path: "$", message: String(__e) }); } const __r = ((__v) => typeof __v !== "number" ? Err({ kind: "StructuralMismatch", path: "$", expected: "integer", actual: typeof __v } as BoundaryError) : Number.isInteger(__v) ? Ok(__v) : Err({ kind: "StructuralMismatch", path: "$", expected: "integer", actual: String(__v) } as BoundaryError))(__j); if (__r.tag === "Ok") return Ok(__r.value as number); const __be = __r.error; return Err({ kind: __be.kind, path: (__be.kind === "StructuralMismatch" || __be.kind === "RefinementViolation") ? __be.path : "$", message: __be.kind === "StructuralMismatch" ? `expected ${__be.expected}, got ${String(__be.actual)}` : __be.kind === "RefinementViolation" ? __be.violation.message : __be.details }); })("42")));
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("decodes a base type")) results.push({ name: "decodes a base type", ...(await test_decodes_a_base_type()) });
  return results;
}
