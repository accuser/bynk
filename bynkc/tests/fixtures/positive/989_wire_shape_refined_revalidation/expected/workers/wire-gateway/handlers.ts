// Generated by bynkc — do not edit by hand.
// context wire.gateway

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../../runtime.js";

import type * as wire_auth from "../wire-auth/handlers.js";

/**
 * #855 anti-drift fixture (`bynkc/tests/wire_shape_drift.rs`): the caller side.
 * It consumes `wire.auth` and calls `login`, so under `--target workers` it
 * generates its **own** codecs for the callee-owned boundary types
 * (`ClientId`, `AuthId`, `Session`, `LoginError`) rather than importing the
 * callee's module as a value — mirroring `404_consumed_type_caller_codecs`'
 * shape, renamed for this fixture's own boundary types.
 */

export type GatewayError =
    { readonly tag: "Rejected" };

export const GatewayError = {
  Rejected: { tag: "Rejected" } as GatewayError,
};

export const enter = {
  /**
   * Admit a request. Maps every login failure onto the gateway's own error, so
   * the callee's `LoginError` never appears in this context's own signature —
   * it is reached only inside the generated
   * `deserialise_Result_Session_LoginError`.
   */
  async call(seed: string, deps: { env: { WIRE_AUTH: ServiceBinding } }): Promise<Result<void, GatewayError>> {
    const s = await callService(deps.env.WIRE_AUTH, "login", seed as JsonValue, deserialise_Result_Session_LoginError, "wire.gateway", "de515c764e6e0e02");
    switch (s.tag) {
      case "Ok": {
        return Ok(undefined);
      }
      case "Err": {
        return Err(GatewayError.Rejected);
      }
    }
    throw new Error("non-exhaustive match");
  },
};

export function serialise_GatewayError(value: GatewayError): JsonValue {
  switch (value.tag) {
    case "Rejected":
      return { kind: "Rejected" };
  }
}

export function deserialise_GatewayError(json: JsonValue, path: string = "$"): Result<GatewayError, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const kind = obj["kind"];
  switch (kind) {
    case "Rejected":
      return Ok({ tag: "Rejected" } as GatewayError);
    default:
      return Err({ kind: "StructuralMismatch", path, expected: "sum variant kind", actual: String(kind) });
  }
}


export function serialise_Result_Unit_GatewayError(value: Result<void, GatewayError>): JsonValue {
  if (value.tag === "Ok") return { kind: "Ok", value: null };
  return { kind: "Err", error: serialise_GatewayError(value.error) };
}

export function deserialise_Result_Unit_GatewayError(json: JsonValue, path: string = "$"): Result<Result<void, GatewayError>, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (obj["kind"] === "Ok") {
  const __v = undefined;
    return Ok(Ok(__v) as Result<void, GatewayError>);
  } else if (obj["kind"] === "Err") {
  const __r_e = deserialise_GatewayError(obj["error"], `${path}.error`);
  if (__r_e.tag === "Err") return __r_e;
  const __e = __r_e.value;
    return Ok(Err(__e) as Result<void, GatewayError>);
  }
  return Err({ kind: "StructuralMismatch", path, expected: "Ok | Err", actual: String(obj["kind"]) });
}

export function serialise_AuthId(value: wire_auth.AuthId): JsonValue {
  return value as unknown as string;
}

export function deserialise_AuthId(json: JsonValue, path: string = "$"): Result<wire_auth.AuthId, BoundaryError> {
  if (typeof json !== "string") {
    return Err({ kind: "StructuralMismatch", path, expected: "string", actual: typeof json });
  }
  return Ok(json as unknown as wire_auth.AuthId);
}

export function serialise_ClientId(value: wire_auth.ClientId): JsonValue {
  return value as unknown as string;
}

export function deserialise_ClientId(json: JsonValue, path: string = "$"): Result<wire_auth.ClientId, BoundaryError> {
  if (typeof json !== "string") {
    return Err({ kind: "StructuralMismatch", path, expected: "string", actual: typeof json });
  }
  if (!(json.length > 0)) {
    return Err({ kind: "RefinementViolation", path, violation: { field: "ClientId", message: "must be non-empty", value: json } });
  }
  if (!(json.length <= 20)) {
    return Err({ kind: "RefinementViolation", path, violation: { field: "ClientId", message: "length must be at most 20", value: json } });
  }
  return Ok(json as unknown as wire_auth.ClientId);
}

export function serialise_LoginError(value: wire_auth.LoginError): JsonValue {
  switch (value.tag) {
    case "Invalid":
      return { kind: "Invalid" };
  }
}

export function deserialise_LoginError(json: JsonValue, path: string = "$"): Result<wire_auth.LoginError, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const kind = obj["kind"];
  switch (kind) {
    case "Invalid":
      return Ok({ tag: "Invalid" } as wire_auth.LoginError);
    default:
      return Err({ kind: "StructuralMismatch", path, expected: "sum variant kind", actual: String(kind) });
  }
}

export function serialise_Session(value: wire_auth.Session): JsonValue {
  return {
    client: serialise_ClientId(value.client),
    auth: serialise_AuthId(value.auth),
  };
}

export function deserialise_Session(json: JsonValue, path: string = "$"): Result<wire_auth.Session, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const __r_client = deserialise_ClientId(obj["client"], `${path}.client`);
  if (__r_client.tag === "Err") return __r_client;
  const __client = __r_client.value;
  const __r_auth = deserialise_AuthId(obj["auth"], `${path}.auth`);
  if (__r_auth.tag === "Err") return __r_auth;
  const __auth = __r_auth.value;
  return Ok({ client: __client, auth: __auth } as wire_auth.Session);
}


export function serialise_Result_Session_LoginError(value: Result<wire_auth.Session, wire_auth.LoginError>): JsonValue {
  if (value.tag === "Ok") return { kind: "Ok", value: serialise_Session(value.value) };
  return { kind: "Err", error: serialise_LoginError(value.error) };
}

export function deserialise_Result_Session_LoginError(json: JsonValue, path: string = "$"): Result<Result<wire_auth.Session, wire_auth.LoginError>, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (obj["kind"] === "Ok") {
  const __r_v = deserialise_Session(obj["value"], `${path}.value`);
  if (__r_v.tag === "Err") return __r_v;
  const __v = __r_v.value;
    return Ok(Ok(__v) as Result<wire_auth.Session, wire_auth.LoginError>);
  } else if (obj["kind"] === "Err") {
  const __r_e = deserialise_LoginError(obj["error"], `${path}.error`);
  if (__r_e.tag === "Err") return __r_e;
  const __e = __r_e.value;
    return Ok(Err(__e) as Result<wire_auth.Session, wire_auth.LoginError>);
  }
  return Err({ kind: "StructuralMismatch", path, expected: "Ok | Err", actual: String(obj["kind"]) });
}

