// Generated by bynkc — do not edit by hand.
// commons demo.optlift

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, HttpResult } from "./runtime.js";

export function lookup(id: number): Option<string> {
  return (id > 0 ? Some("found") : None);
}

export function handler(id: number): HttpResult<string> {
  const __r0 = lookup(id);
  if (__r0.tag === "None") return HttpResult.NotFound;
  const name = __r0.value;
  return HttpResult.Ok(name);
}

export async function asyncHandler(id: number): Promise<HttpResult<string>> {
  const __r0 = lookup(id);
  if (__r0.tag === "None") return HttpResult.NotFound;
  const name = __r0.value;
  return HttpResult.Ok(name);
}

export async function readOpt(id: number): Promise<Option<string>> {
  return lookup(id);
}

export async function kvHandler(id: number): Promise<HttpResult<string>> {
  const stored = await readOpt(id);
  const __r0 = stored;
  if (__r0.tag === "None") return HttpResult.NotFound;
  const name = __r0.value;
  return HttpResult.Ok(name);
}

