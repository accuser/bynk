// Generated by bynkc — do not edit by hand.
// test target: app.greet

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "../runtime.js";
import * as app_greet from "./../app/greet.js";
import * as bynk from "./../bynk.js";
import * as bynk_locale from "./../bynk/locale.js";
import * as bynk_locale_types from "./../bynk/locale/types.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function __bynkDeepEqual(a: unknown, b: unknown): boolean {
  const s = (v: unknown) => JSON.stringify(v, (_k, val) => typeof val === "bigint" ? "__bigint__" + String(val) : val);
  try { return s(a) === s(b); } catch { return a === b; }
}

class __Stub_Locale {
  async current(): Promise<bynk_locale_types.LocaleTag> {
    if (true) {
      return ("fr" as any);
    }
    throw new Error("bynk: no stub clause matched for Locale.current");
  }
}

function makeTestDeps() {
  return { Locale: new __Stub_Locale() };
}

// case tier: integration
async function test_a_stubbed_locale_is_what_the_capability_reports() {
  try {
    const deps = makeTestDeps();
    const { currentTag, greeting } = app_greet as any;
    const { message, render, renderArg, withMoment, withNum, withText, withWhole } = bynk_locale as any;
    const { LocaleTag, Message, MessageArg } = bynk_locale_types as any;
    const { FetchError, Method, Request, Response, Uuid } = bynk as any;
    const tag = await currentTag.call(deps);
    if (!(tag === "fr")) { throw __bynkExpectFailure("tests/greet.test.bynk:11:12", 357, 368, "expect tag == \"fr\"\n  expected: tag == \"fr\"\n  actual:   " + __bynkShow((tag)) + " == " + __bynkShow(("fr"))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: integration
async function test_render_is_unaffected_by_the_stub_in_slice_1__tag_is_unused_() {
  try {
    const deps = makeTestDeps();
    const { currentTag, greeting } = app_greet as any;
    const { message, render, renderArg, withMoment, withNum, withText, withWhole } = bynk_locale as any;
    const { LocaleTag, Message, MessageArg } = bynk_locale_types as any;
    const { FetchError, Method, Request, Response, Uuid } = bynk as any;
    const g = await greeting.call(deps);
    if (!(g === "hello")) { throw __bynkExpectFailure("tests/greet.test.bynk:16:12", 485, 497, "expect g == \"hello\"\n  expected: g == \"hello\"\n  actual:   " + __bynkShow((g)) + " == " + __bynkShow(("hello"))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("a stubbed locale is what the capability reports")) results.push({ name: "a stubbed locale is what the capability reports", ...(await test_a_stubbed_locale_is_what_the_capability_reports()) });
  if (want("render is unaffected by the stub in slice 1 (tag is unused)")) results.push({ name: "render is unaffected by the stub in slice 1 (tag is unused)", ...(await test_render_is_unaffected_by_the_stub_in_slice_1__tag_is_unused_()) });
  return results;
}
