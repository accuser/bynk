suite commerce.payment {
  provides Logger.log(_) returns ()

  case "positive amount authorises" {
    let result <- authorise.call(100)
    expect result is Ok(_)
  }

  case "zero amount is declined" {
    let result <- authorise.call(0)
    expect result is Err(_)
  }
}
