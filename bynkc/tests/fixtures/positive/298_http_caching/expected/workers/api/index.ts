// Generated by bynkc — do not edit by hand.
// Worker entry point for context `api`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError, matchPath, httpResultToResponse, headResponse, notModifiedIfMatch, applyCache, type CorsPolicy, applyCors, corsPreflightResponse, type SecurityPolicy, applySecurityHeaders } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    const __cors_api: CorsPolicy = { origins: ["https://app.example.com"], allowMethods: ["GET", "HEAD", "OPTIONS", "POST"], allowHeaders: ["content-type"], credentials: false, maxAgeSecs: 3600 };
    const __security_api: SecurityPolicy = { nosniff: true, hstsMaxAgeSecs: null };
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      if (method === "OPTIONS" && request.headers.get("access-control-request-method") !== null && (path === "/config" || path === "/plain" || path === "/private" || path === "/ticks" || path === "/items")) {
        return applySecurityHeaders(corsPreflightResponse(__cors_api, request.headers.get("origin")), __security_api);
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/config") {
          const result = await surface.http_GET_config();
          const __response = applySecurityHeaders(applyCors(notModifiedIfMatch(applyCache(httpResultToResponse(result, (v: any) => v as JsonValue, { weakEtag: true }), 300, "public"), request), __cors_api, request.headers.get("origin")), __security_api);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/plain") {
          const result = await surface.http_GET_plain();
          const __response = applySecurityHeaders(applyCors(notModifiedIfMatch(httpResultToResponse(result, (v: any) => v as JsonValue, { weakEtag: true }), request), __cors_api, request.headers.get("origin")), __security_api);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/private") {
          const result = await surface.http_GET_private();
          const __response = applySecurityHeaders(applyCors(notModifiedIfMatch(applyCache(httpResultToResponse(result, (v: any) => v as JsonValue, { weakEtag: true }), 30, "private"), request), __cors_api, request.headers.get("origin")), __security_api);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/ticks") {
          const result = await surface.http_GET_ticks();
          const __response = applySecurityHeaders(applyCors(notModifiedIfMatch(httpResultToResponse(result, (_v: any) => null, { weakEtag: true }), request), __cors_api, request.headers.get("origin")), __security_api);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if (method === "POST" && path === "/items") {
          let __body_json: JsonValue;
          try {
            __body_json = (await request.json()) as JsonValue;
          } catch {
            return applySecurityHeaders(applyCors(new Response(JSON.stringify({ kind: "MalformedJson", details: "Invalid request body" }), { status: 400, headers: { "content-type": "application/json" } }), __cors_api, request.headers.get("origin")), __security_api);
          }
          const __r_body = ((__v) => typeof __v === "string" ? Ok(__v) : Err({ kind: "StructuralMismatch", path: "$", expected: "string", actual: typeof __v } as BoundaryError))(__body_json);
          if (__r_body.tag === "Err") return applySecurityHeaders(applyCors(new Response(JSON.stringify(__r_body.error), { status: 400, headers: { "content-type": "application/json" } }), __cors_api, request.headers.get("origin")), __security_api);
          const body = __r_body.value;
          const result = await surface.http_POST_items(body);
          return applySecurityHeaders(applyCors(httpResultToResponse(result, (v: any) => v as JsonValue), __cors_api, request.headers.get("origin")), __security_api);
        }
      }
      if (path === "/config") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(applyCors(__res, __cors_api, request.headers.get("origin")), __security_api);
      }
      if (path === "/items") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "OPTIONS, POST" } });
        return applySecurityHeaders(applyCors(__res, __cors_api, request.headers.get("origin")), __security_api);
      }
      if (path === "/plain") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(applyCors(__res, __cors_api, request.headers.get("origin")), __security_api);
      }
      if (path === "/private") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(applyCors(__res, __cors_api, request.headers.get("origin")), __security_api);
      }
      if (path === "/ticks") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(applyCors(__res, __cors_api, request.headers.get("origin")), __security_api);
      }
      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
