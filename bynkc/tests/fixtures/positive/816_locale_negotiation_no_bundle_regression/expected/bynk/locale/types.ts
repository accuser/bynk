// Generated by bynkc — do not edit by hand.
// commons bynk.locale.types

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../../runtime.js";

/**
 * A well-formed BCP-47 language tag: `language[-extlang][-Script][-REGION]
 * [-variant]*[-extension]*[-privateuse]`, or a standalone `x-...` private-use
 * tag (e.g. `en`, `pt-BR`, `zh-Hans-CN`, `zh-yue`, `ca-valencia`,
 * `de-CH-1996`, `en-US-u-ca-buddhist`, `de-CH-x-phonebk`, `x-custom`).
 * Casing is canonical by construction: language/extlang/variant/extension/
 * private-use lowercase, script Titlecase, region uppercase (or 3 digits).
 * Grandfathered/irregular tags (`i-klingon`, `en-GB-oed`) don't fit this
 * productive shape and are rejected. This checks well-formedness only, not
 * registry membership — `en-abc` (a made-up extlang shape) is well-formed
 * and accepted, and extlang position isn't restricted to a 2-letter primary
 * the way RFC 5646 restricts it.
 */
export type LocaleTag = string & { readonly __brand: "LocaleTag" };

export const LocaleTag = {
  of(value: string): Result<LocaleTag, ValidationError> {
    if (!new RegExp("^(?:" + "(?:[a-z]{2,3}(?:-[a-z]{3}){0,3}(?:-[A-Z][a-z]{3})?(?:-(?:[A-Z]{2}|[0-9]{3}))?(?:-(?:[a-z0-9]{5,8}|[0-9][a-z0-9]{3}))*(?:-[a-wy-z0-9](?:-[a-z0-9]{2,8}){1,8})*(?:-x(?:-[a-z0-9]{1,8}){1,8})?|x(?:-[a-z0-9]{1,8}){1,8})" + ")$").test(value)) {
      return Err({ field: "LocaleTag", message: "must match /(?:[a-z]{2,3}(?:-[a-z]{3}){0,3}(?:-[A-Z][a-z]{3})?(?:-(?:[A-Z]{2}|[0-9]{3}))?(?:-(?:[a-z0-9]{5,8}|[0-9][a-z0-9]{3}))*(?:-[a-wy-z0-9](?:-[a-z0-9]{2,8}){1,8})*(?:-x(?:-[a-z0-9]{1,8}){1,8})?|x(?:-[a-z0-9]{1,8}){1,8})/", value });
    }
    return Ok(value as LocaleTag);
  },
};

/**
 * One value substitutable into a rendered message.
 */
export type MessageArg =
    { readonly tag: "Text"; readonly value: string }
  | { readonly tag: "Whole"; readonly value: number }
  | { readonly tag: "Num"; readonly value: number }
  | { readonly tag: "Moment"; readonly value: number };

export const MessageArg = {
  Text: (value: string): MessageArg => ({ tag: "Text", value }),
  Whole: (value: number): MessageArg => ({ tag: "Whole", value }),
  Num: (value: number): MessageArg => ({ tag: "Num", value }),
  Moment: (value: number): MessageArg => ({ tag: "Moment", value }),
};

/**
 * A message to render: a lookup code plus its named substitution values.
 */
export interface Message {
  readonly code: string;
  readonly params: ReadonlyMap<string, MessageArg>;
}

export const Message = {
};

export function serialise_LocaleTag(value: LocaleTag): JsonValue {
  return value as unknown as string;
}

export function deserialise_LocaleTag(json: JsonValue, path: string = "$"): Result<LocaleTag, BoundaryError> {
  if (typeof json !== "string") {
    return Err({ kind: "StructuralMismatch", path, expected: "string", actual: typeof json });
  }
  const validated = (typeof (LocaleTag as any).of === "function")
    ? (LocaleTag as any).of(json)
    : Ok(json as unknown as LocaleTag);
  if (validated.tag === "Err") {
    return Err({ kind: "RefinementViolation", path, violation: validated.error });
  }
  return Ok(validated.value as LocaleTag);
}

export function serialise_Message(value: Message): JsonValue {
  return {
    code: value.code as JsonValue,
    params: serialise_Map_String_MessageArg(value.params),
  };
}

export function deserialise_Message(json: JsonValue, path: string = "$"): Result<Message, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (typeof obj["code"] !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}.code`, expected: "string", actual: typeof obj["code"] });
  }
  const __code = obj["code"];
  const __r_params = deserialise_Map_String_MessageArg(obj["params"], `${path}.params`);
  if (__r_params.tag === "Err") return __r_params;
  const __params = __r_params.value;
  return Ok({ code: __code, params: __params } as Message);
}

export function serialise_MessageArg(value: MessageArg): JsonValue {
  switch (value.tag) {
    case "Text": {
      return { kind: "Text", value: (value as any).value as JsonValue };
    }
    case "Whole": {
      return { kind: "Whole", value: (value as any).value as JsonValue };
    }
    case "Num": {
      return { kind: "Num", value: ((v: number) => { if (!Number.isFinite(v)) throw new Error("non-finite Float at boundary"); return v as JsonValue; })((value as any).value) };
    }
    case "Moment": {
      return { kind: "Moment", value: (value as any).value as JsonValue };
    }
  }
}

export function deserialise_MessageArg(json: JsonValue, path: string = "$"): Result<MessageArg, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const kind = obj["kind"];
  switch (kind) {
    case "Text": {
  if (typeof obj["value"] !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}.value`, expected: "string", actual: typeof obj["value"] });
  }
  const __value = obj["value"];
      return Ok({ tag: "Text", value: __value } as MessageArg);
    }
    case "Whole": {
  if (typeof obj["value"] !== "number") {
    return Err({ kind: "StructuralMismatch", path: `${path}.value`, expected: "number", actual: typeof obj["value"] });
  }
  if (!Number.isInteger(obj["value"])) {
    return Err({ kind: "StructuralMismatch", path: `${path}.value`, expected: "integer", actual: String(obj["value"]) });
  }
  const __value = obj["value"];
      return Ok({ tag: "Whole", value: __value } as MessageArg);
    }
    case "Num": {
  if (typeof obj["value"] !== "number") {
    return Err({ kind: "StructuralMismatch", path: `${path}.value`, expected: "number", actual: typeof obj["value"] });
  }
  if (!Number.isFinite(obj["value"])) {
    return Err({ kind: "StructuralMismatch", path: `${path}.value`, expected: "finite number", actual: String(obj["value"]) });
  }
  const __value = obj["value"];
      return Ok({ tag: "Num", value: __value } as MessageArg);
    }
    case "Moment": {
  if (typeof obj["value"] !== "number") {
    return Err({ kind: "StructuralMismatch", path: `${path}.value`, expected: "number", actual: typeof obj["value"] });
  }
  if (!Number.isInteger(obj["value"])) {
    return Err({ kind: "StructuralMismatch", path: `${path}.value`, expected: "integer", actual: String(obj["value"]) });
  }
  const __value = obj["value"];
      return Ok({ tag: "Moment", value: __value } as MessageArg);
    }
    default:
      return Err({ kind: "StructuralMismatch", path, expected: "sum variant kind", actual: String(kind) });
  }
}


export function serialise_Map_String_MessageArg(value: ReadonlyMap<string, MessageArg>): JsonValue {
  const entries: JsonValue[] = [];
  for (const [k, v] of value) {
    entries.push([k as JsonValue, serialise_MessageArg(v)]);
  }
  return entries;
}

export function deserialise_Map_String_MessageArg(json: JsonValue, path: string = "$"): Result<ReadonlyMap<string, MessageArg>, BoundaryError> {
  if (!Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "array", actual: typeof json });
  }
  const out = new Map<string, MessageArg>();
  for (let i = 0; i < json.length; i++) {
  const entry = json[i];
  if (!Array.isArray(entry) || entry.length !== 2) {
    return Err({ kind: "StructuralMismatch", path: `${path}[${i}]`, expected: "[key, value] entry", actual: typeof entry });
  }
  const entryK = entry[0];
  const entryV = entry[1];
  if (typeof entryK !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}[${i}][0]`, expected: "string", actual: typeof entryK });
  }
  const __k = entryK;
  const __r_v = deserialise_MessageArg(entryV, `${path}[${i}][1]`);
  if (__r_v.tag === "Err") return __r_v;
  const __v = __r_v.value;
  out.set(__k as string, __v as MessageArg);
  }
  return Ok(out);
}

