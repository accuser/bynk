// Generated by bynkc — do not edit by hand.
// Worker entry point for context `assets`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError, matchPath, httpResultToResponse, headResponse, notModifiedIfMatch, type SecurityPolicy, applySecurityHeaders } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    const __security_Assets: SecurityPolicy = { nosniff: true, hstsMaxAgeSecs: null };
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      {
        if ((method === "GET" || method === "HEAD") && path === "/pixel.png") {
          const result = await surface.http_GET_pixel_png();
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (_v: any) => null, { weakEtag: true }), request), __security_Assets);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/sitemap.xml") {
          const result = await surface.http_GET_sitemap_xml();
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (_v: any) => null, { weakEtag: true }), request), __security_Assets);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        const __m = matchPath("/robots/:mode", path);
        if ((method === "GET" || method === "HEAD") && __m) {
          const __raw_mode = __m.params["mode"];
          const mode = __raw_mode;
          const result = await surface.http_GET_robots_Param_mode(mode);
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (_v: any) => null, { weakEtag: true }), request), __security_Assets);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      if (path === "/pixel.png") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_Assets);
      }
      if (matchPath("/robots/:mode", path) !== null) {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_Assets);
      }
      if (path === "/sitemap.xml") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_Assets);
      }
      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
