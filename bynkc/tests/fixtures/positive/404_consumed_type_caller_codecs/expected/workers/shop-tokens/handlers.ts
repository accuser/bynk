// Generated by bynkc — do not edit by hand.
// context shop.tokens

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../../runtime.js";

/**
 * Tokens context. Mints access grants. The callee whose boundary types a
 * consumer must generate its own codecs for (#661): an `exports opaque` type
 * (the caller casts structurally — Decision C) and an `exports transparent`
 * refined type (the caller inlines the predicate — Decision D).
 */

export type Token = string & { readonly __brand: "shop.tokens.Token" };

export const Token = {
  of(value: string): Result<Token, ValidationError> {
    if (!(value.length > 0)) {
      return Err({ field: "Token", message: "must be non-empty", value });
    }
    return Ok(value as Token);
  },
  unsafe(value: string): Token {
    return value as Token;
  },
};

export type Score = number & { readonly __brand: "shop.tokens.Score" };

export const Score = {
  of(value: number): Result<Score, ValidationError> {
    if (!Number.isInteger(value)) {
      return Err({ field: "Score", message: "must be an integer", value });
    }
    if (!(value >= 0 && value <= 100)) {
      return Err({ field: "Score", message: "must be in range [0, 100]", value });
    }
    return Ok(value as Score);
  },
};

export interface Grant {
  readonly token: Token;
  readonly score: Score;
}

export const Grant = {
};

export type TokenError =
    { readonly tag: "Denied" }
  | { readonly tag: "Expired" };

export const TokenError = {
  Denied: { tag: "Denied" } as TokenError,
  Expired: { tag: "Expired" } as TokenError,
};

/**
 * Mint a grant for a seed. The single cross-context entry point.
 */
export const issue = {
  async call(seed: string, deps: {}): Promise<Result<Grant, TokenError>> {
    const __r0 = Score.of(50);
    switch (__r0.tag) {
      case "Ok": {
        const s = __r0.value;
        return Ok({ token: Token.unsafe("tok"), score: s });
      }
      case "Err": {
        return Err(TokenError.Denied);
      }
    }
    throw new Error("non-exhaustive match");
  },
};

export function serialise_Grant(value: Grant): JsonValue {
  return {
    token: serialise_Token(value.token),
    score: serialise_Score(value.score),
  };
}

export function deserialise_Grant(json: JsonValue, path: string = "$"): Result<Grant, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const __r_token = deserialise_Token(obj["token"], `${path}.token`);
  if (__r_token.tag === "Err") return __r_token;
  const __token = __r_token.value;
  const __r_score = deserialise_Score(obj["score"], `${path}.score`);
  if (__r_score.tag === "Err") return __r_score;
  const __score = __r_score.value;
  return Ok({ token: __token, score: __score } as Grant);
}

export function serialise_Score(value: Score): JsonValue {
  return value as unknown as number;
}

export function deserialise_Score(json: JsonValue, path: string = "$"): Result<Score, BoundaryError> {
  if (typeof json !== "number") {
    return Err({ kind: "StructuralMismatch", path, expected: "number", actual: typeof json });
  }
  const validated = (typeof (Score as any).of === "function")
    ? (Score as any).of(json)
    : Ok(json as unknown as Score);
  if (validated.tag === "Err") {
    return Err({ kind: "RefinementViolation", path, violation: validated.error });
  }
  return Ok(validated.value as Score);
}

export function serialise_Token(value: Token): JsonValue {
  return value as unknown as string;
}

export function deserialise_Token(json: JsonValue, path: string = "$"): Result<Token, BoundaryError> {
  if (typeof json !== "string") {
    return Err({ kind: "StructuralMismatch", path, expected: "string", actual: typeof json });
  }
  const validated = (typeof (Token as any).of === "function")
    ? (Token as any).of(json)
    : Ok(json as unknown as Token);
  if (validated.tag === "Err") {
    return Err({ kind: "RefinementViolation", path, violation: validated.error });
  }
  return Ok(validated.value as Token);
}

export function serialise_TokenError(value: TokenError): JsonValue {
  switch (value.tag) {
    case "Denied":
      return { kind: "Denied" };
    case "Expired":
      return { kind: "Expired" };
  }
}

export function deserialise_TokenError(json: JsonValue, path: string = "$"): Result<TokenError, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const kind = obj["kind"];
  switch (kind) {
    case "Denied":
      return Ok({ tag: "Denied" } as TokenError);
    case "Expired":
      return Ok({ tag: "Expired" } as TokenError);
    default:
      return Err({ kind: "StructuralMismatch", path, expected: "sum variant kind", actual: String(kind) });
  }
}


export function serialise_Result_Grant_TokenError(value: Result<Grant, TokenError>): JsonValue {
  if (value.tag === "Ok") return { kind: "Ok", value: serialise_Grant(value.value) };
  return { kind: "Err", error: serialise_TokenError(value.error) };
}

export function deserialise_Result_Grant_TokenError(json: JsonValue, path: string = "$"): Result<Result<Grant, TokenError>, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (obj["kind"] === "Ok") {
  const __r_v = deserialise_Grant(obj["value"], `${path}.value`);
  if (__r_v.tag === "Err") return __r_v;
  const __v = __r_v.value;
    return Ok(Ok(__v) as Result<Grant, TokenError>);
  } else if (obj["kind"] === "Err") {
  const __r_e = deserialise_TokenError(obj["error"], `${path}.error`);
  if (__r_e.tag === "Err") return __r_e;
  const __e = __r_e.value;
    return Ok(Err(__e) as Result<Grant, TokenError>);
  }
  return Err({ kind: "StructuralMismatch", path, expected: "Ok | Err", actual: String(obj["kind"]) });
}

