// Generated by bynkc — do not edit by hand.
// context commerce.order

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type DurableObjectState, type DurableObjectNamespace, StateRegistry, makeAgent, invariantViolation, rehydrationViolation, type JsonValue, type BoundaryError } from "../runtime.js";

/**
 * Review of #1374: `commitState`'s own invariant/transition predicates
 * normally lower to a pure expression with nothing hoisted, invisible in
 * every pre-existing fixture. `if true { 5 is PositiveInt } else { false }`
 * (the same shape `1029_agent_static_init_hoist` already proved hoists a
 * receiver temp) forces a real hoisted pre-statement ahead of both the
 * invariant's own top-level check and the transition's own nested one, so
 * the two different `TsStmt::Raw` indent depths this conversion computes
 * are both exercised by a real fixture, not just a `bynk-ts` unit test.
 */

export type PositiveInt = number & { readonly __brand: "commerce.order.PositiveInt" };

export const PositiveInt = {
  of(value: number): Result<PositiveInt, ValidationError> {
    if (!Number.isInteger(value)) {
      return Err({ field: "PositiveInt", message: "must be an integer", value });
    }
    if (!(value > 0)) {
      return Err({ field: "PositiveInt", message: "must be positive", value });
    }
    return Ok(value as PositiveInt);
  },
};

export type OrderStatus =
    { readonly tag: "Pending" }
  | { readonly tag: "Placed" }
  | { readonly tag: "Paid" };

export const OrderStatus = {
  Pending: { tag: "Pending" } as OrderStatus,
  Placed: { tag: "Placed" } as OrderStatus,
  Paid: { tag: "Paid" } as OrderStatus,
};

export interface OrderState {
  readonly status: OrderStatus;
}

const __OrderRegistry = new StateRegistry();
function __zeroOfOrderState(): OrderState { return { status: OrderStatus.Pending }; }

function __rehydrateOrderState(s: OrderState): void {
  { const __r = deserialise_OrderStatus((s.status as unknown as JsonValue), "status"); if (__r.tag === "Err") throw rehydrationViolation("Order", __r.error); }
}

export class Order {
  state: DurableObjectState;
  constructor(state: DurableObjectState) {
    this.state = state;
  }

  private async loadState(): Promise<OrderState> {
    const stored = await this.state.storage.get<OrderState>("state");
    if (stored === undefined) return __zeroOfOrderState();
    const __merged = { ...__zeroOfOrderState(), ...stored };
    __rehydrateOrderState(__merged);
    return __merged;
  }

  private async commitState(s: OrderState): Promise<void> {
    let __r1: boolean;
    if (true) {
      const __r0 = 5;
      __r1 = (Number.isInteger(__r0) && __r0 > 0);
    } else {
      __r1 = false;
    }
    if (!(__r1)) {
      console.error("InvariantViolation Order.hoisting_invariant", { agent: "Order", invariant: "hoisting_invariant" });
      throw invariantViolation("Order", "hoisting_invariant");
    }
    const __prior = await this.state.storage.get<OrderState>("state");
    if (__prior !== undefined) {
      const __old = { ...__zeroOfOrderState(), ...__prior };
      const __new = s;
      let __r1: boolean;
      if (__old.status === __old.status) {
        const __r0 = 5;
        __r1 = (Number.isInteger(__r0) && __r0 > 0);
      } else {
        __r1 = false;
      }
      if (!(__r1)) {
        console.error("InvariantViolation Order.hoisting_transition", { agent: "Order", invariant: "hoisting_transition" });
        throw invariantViolation("Order", "hoisting_transition");
      }
    }
    await this.state.storage.put("state", s);
  }

  async place(deps: {}): Promise<void> {
    const __state = { ...(await this.loadState()) };
    const __result = await (async () => {
      __state.status = OrderStatus.Placed;
      return undefined;
    })();
    await this.commitState(__state);
    return __result;
  }

}

export function __makeOrder(key: string, env?: { ORDER?: DurableObjectNamespace }): Order {
  return makeAgent(__OrderRegistry, env?.ORDER, key, (state) => new Order(state));
}

export function __resetAgents(): void {
  __OrderRegistry.reset();
}

export function serialise_OrderStatus(value: OrderStatus): JsonValue {
  switch (value.tag) {
    case "Pending":
      return { kind: "Pending" };
    case "Placed":
      return { kind: "Placed" };
    case "Paid":
      return { kind: "Paid" };
  }
}

export function deserialise_OrderStatus(json: JsonValue, path: string = "$"): Result<OrderStatus, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const kind = obj["kind"];
  switch (kind) {
    case "Pending":
      return Ok({ tag: "Pending" } as OrderStatus);
    case "Placed":
      return Ok({ tag: "Placed" } as OrderStatus);
    case "Paid":
      return Ok({ tag: "Paid" } as OrderStatus);
    default:
      return Err({ kind: "StructuralMismatch", path, expected: "sum variant kind", actual: String(kind) });
  }
}


