// Generated by bynkc — do not edit by hand.
// Worker entry point for context `api`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError, matchPath, httpResultToResponse, headResponse, notModifiedIfMatch, type SecurityPolicy, applySecurityHeaders } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    const __security_routes: SecurityPolicy = { nosniff: true, hstsMaxAgeSecs: null };
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      {
        if ((method === "GET" || method === "HEAD") && path === "/logo") {
          const result = await surface.http_GET_logo();
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (_v: any) => null, { weakEtag: true }), request), __security_routes);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/notes") {
          const result = await surface.http_GET_notes();
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (v: any) => v as JsonValue, { weakEtag: true }), request), __security_routes);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/ticks") {
          const result = await surface.http_GET_ticks();
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (_v: any) => null, { weakEtag: true }), request), __security_routes);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        if (method === "POST" && path === "/notes") {
          let __body_json: JsonValue;
          try {
            __body_json = (await request.json()) as JsonValue;
          } catch {
            return new Response(JSON.stringify({ kind: "MalformedJson", details: "Invalid request body" }), { status: 400, headers: { "content-type": "application/json" } });
          }
          const __r_body = ((__v) => typeof __v === "string" ? Ok(__v) : Err({ kind: "StructuralMismatch", path: "$", expected: "string", actual: typeof __v } as BoundaryError))(__body_json);
          if (__r_body.tag === "Err") return new Response(JSON.stringify(__r_body.error), { status: 400, headers: { "content-type": "application/json" } });
          const body = __r_body.value;
          const result = await surface.http_POST_notes(body);
          return applySecurityHeaders(httpResultToResponse(result, (v: any) => v as JsonValue), __security_routes);
        }
      }
      {
        const __m = matchPath("/notes/:id", path);
        if ((method === "GET" || method === "HEAD") && __m) {
          const __raw_id = __m.params["id"];
          const id = __raw_id;
          const result = await surface.http_GET_notes_Param_id(id);
          const __response = applySecurityHeaders(notModifiedIfMatch(httpResultToResponse(result, (v: any) => v as JsonValue, { weakEtag: true }), request), __security_routes);
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      if (path === "/logo") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_routes);
      }
      if (path === "/notes") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS, POST" } });
        return applySecurityHeaders(__res, __security_routes);
      }
      if (matchPath("/notes/:id", path) !== null) {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_routes);
      }
      if (path === "/ticks") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applySecurityHeaders(__res, __security_routes);
      }
      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
