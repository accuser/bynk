// Generated by bynkc — do not edit by hand.
// commons demo

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "./runtime.js";

export type Email = string & { readonly __brand: "Email" };

export const Email = {
  of(value: string): Result<Email, ValidationError> {
    if (!(value.length > 0)) {
      return Err({ field: "Email", message: "must be non-empty", value });
    }
    return Ok(value as Email);
  },
};

export function pick(r: Result<Email, string>): string {
  switch (r.tag) {
    case "Ok": {
      const e = r.value;
      return e;
    }
    case "Err": {
      const m = r.error;
      return m;
    }
  }
  throw new Error("non-exhaustive match");
}

export function choose(flag: boolean, e: Email, s: string): string {
  return (flag ? e : s);
}

