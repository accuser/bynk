// Generated by bynkc — do not edit by hand.
// test target: shipping.rates

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "../runtime.js";
import * as shipping_rates from "./../shipping/rates.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function makeTestDeps() {
  return {  };
}

// case tier: unit
async function test_rate_rejects_negative() {
  try {
    const deps = {};
    const { Rate } = shipping_rates as any;
    const r = Rate.fromInt(-1);
    if (!(r.tag === "Err")) { throw __bynkExpectFailure("tests/rates.test.bynk:4:12", 96, 107, "expect r is Err(_)"); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_rate_accepts_non_negative() {
  try {
    const deps = {};
    const { Rate } = shipping_rates as any;
    const r = Rate.fromInt(3);
    if (!(r.tag === "Ok")) { throw __bynkExpectFailure("tests/rates.test.bynk:9:12", 189, 199, "expect r is Ok(_)"); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("rate rejects negative")) results.push({ name: "rate rejects negative", ...(await test_rate_rejects_negative()) });
  if (want("rate accepts non-negative")) results.push({ name: "rate accepts non-negative", ...(await test_rate_accepts_non_negative()) });
  return results;
}
