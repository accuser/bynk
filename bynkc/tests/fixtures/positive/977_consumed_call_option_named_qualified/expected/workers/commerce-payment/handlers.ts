// Generated by bynkc — do not edit by hand.
// context commerce.payment

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../../runtime.js";

export type Region =
    { readonly tag: "Domestic" }
  | { readonly tag: "International" };

export const Region = {
  Domestic: { tag: "Domestic" } as Region,
  International: { tag: "International" } as Region,
};

export interface Receipt {
  readonly id: string;
  readonly region: Option<Region>;
}

export const Receipt = {
};

export const authorise = {
  async call(id: string, deps: {}): Promise<Result<Receipt, number>> {
    return Ok({ id: id, region: None });
  },
};

export function serialise_Receipt(value: Receipt): JsonValue {
  return {
    id: value.id as JsonValue,
    region: serialise_Option_Region(value.region),
  };
}

export function deserialise_Receipt(json: JsonValue, path: string = "$"): Result<Receipt, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (typeof obj["id"] !== "string") {
    return Err({ kind: "StructuralMismatch", path: `${path}.id`, expected: "string", actual: typeof obj["id"] });
  }
  const __id = obj["id"];
  const __r_region = deserialise_Option_Region(obj["region"], `${path}.region`);
  if (__r_region.tag === "Err") return __r_region;
  const __region = __r_region.value;
  return Ok({ id: __id, region: __region } as Receipt);
}

export function serialise_Region(value: Region): JsonValue {
  switch (value.tag) {
    case "Domestic":
      return { kind: "Domestic" };
    case "International":
      return { kind: "International" };
  }
}

export function deserialise_Region(json: JsonValue, path: string = "$"): Result<Region, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  const kind = obj["kind"];
  switch (kind) {
    case "Domestic":
      return Ok({ tag: "Domestic" } as Region);
    case "International":
      return Ok({ tag: "International" } as Region);
    default:
      return Err({ kind: "StructuralMismatch", path, expected: "sum variant kind", actual: String(kind) });
  }
}


export function serialise_Result_Receipt_Int(value: Result<Receipt, number>): JsonValue {
  if (value.tag === "Ok") return { kind: "Ok", value: serialise_Receipt(value.value) };
  return { kind: "Err", error: value.error as JsonValue };
}

export function deserialise_Result_Receipt_Int(json: JsonValue, path: string = "$"): Result<Result<Receipt, number>, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (obj["kind"] === "Ok") {
  const __r_v = deserialise_Receipt(obj["value"], `${path}.value`);
  if (__r_v.tag === "Err") return __r_v;
  const __v = __r_v.value;
    return Ok(Ok(__v) as Result<Receipt, number>);
  } else if (obj["kind"] === "Err") {
  if (typeof obj["error"] !== "number") {
    return Err({ kind: "StructuralMismatch", path: `${path}.error`, expected: "number", actual: typeof obj["error"] });
  }
  if (!Number.isInteger(obj["error"])) {
    return Err({ kind: "StructuralMismatch", path: `${path}.error`, expected: "integer", actual: String(obj["error"]) });
  }
  const __e = obj["error"];
    return Ok(Err(__e) as Result<Receipt, number>);
  }
  return Err({ kind: "StructuralMismatch", path, expected: "Ok | Err", actual: String(obj["kind"]) });
}

export function serialise_Option_Region(value: Option<Region>): JsonValue {
  if (value.tag === "Some") return { kind: "Some", value: serialise_Region(value.value) };
  return { kind: "None" };
}

export function deserialise_Option_Region(json: JsonValue, path: string = "$"): Result<Option<Region>, BoundaryError> {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Err({ kind: "StructuralMismatch", path, expected: "object", actual: typeof json });
  }
  const obj = json as { [k: string]: JsonValue };
  if (obj["kind"] === "Some") {
  const __r_v = deserialise_Region(obj["value"], `${path}.value`);
  if (__r_v.tag === "Err") return __r_v;
  const __v = __r_v.value;
    return Ok(Some(__v) as Option<Region>);
  } else if (obj["kind"] === "None") {
    return Ok(None as Option<Region>);
  }
  return Err({ kind: "StructuralMismatch", path, expected: "Some | None", actual: String(obj["kind"]) });
}

