// Generated by bynkc — do not edit by hand.
// Worker entry point for context `commerce.order`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export { __EventsFanout } from "./events_fanout.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          case "markPaid": {
            const __contract = request.headers.get("X-Bynk-Contract");
            if (__contract !== "f754ed184415d345") return new Response(JSON.stringify({ kind: "ContractMismatch", service: "markPaid", expected: "f754ed184415d345", actual: __contract }), { status: 409, headers: { "content-type": "application/json" } });
            const args = await request.json() as JsonValue;
            const __r_orderId = ((__v) => typeof __v === "string" ? Ok(__v) : Err({ kind: "StructuralMismatch", path: "$", expected: "string", actual: typeof __v } as BoundaryError))(args);
            if (__r_orderId.tag === "Err") return new Response(JSON.stringify(__r_orderId.error), { status: 400, headers: { "content-type": "application/json" } });
            const orderId = __r_orderId.value;
            const result = await surface.markPaid(orderId);
            const body = null;
            return new Response(JSON.stringify(body), { status: 200, headers: { "content-type": "application/json" } });
          }
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
