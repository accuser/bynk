// Generated by bynkc — do not edit by hand.
// Worker entry point for context `api`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError, matchPath, httpResultToResponse, type CorsPolicy, applyCors, corsPreflightResponse } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    const __cors_api: CorsPolicy = { origins: ["https://app.example.com"], allowMethods: ["GET", "OPTIONS"], allowHeaders: ["content-type"], credentials: false, maxAgeSecs: 3600 };
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      if (method === "OPTIONS" && (path === "/ping" || matchPath("/items/:id", path) !== null)) {
        return corsPreflightResponse(__cors_api, request.headers.get("origin"));
      }
      {
        if (method === "GET" && path === "/ping") {
          const result = await surface.http_GET_ping();
          return applyCors(httpResultToResponse(result, (v: any) => v as JsonValue), __cors_api, request.headers.get("origin"));
        }
      }
      {
        const __m = matchPath("/items/:id", path);
        if (method === "GET" && __m) {
          const __raw_id = __m.params["id"];
          const id = __raw_id;
          const result = await surface.http_GET_items_Param_id(id);
          return applyCors(httpResultToResponse(result, (v: any) => v as JsonValue), __cors_api, request.headers.get("origin"));
        }
      }
      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
