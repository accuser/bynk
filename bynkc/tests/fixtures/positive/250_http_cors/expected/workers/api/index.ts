// Generated by bynkc — do not edit by hand.
// Worker entry point for context `api`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError, matchPath, httpResultToResponse, headResponse, type CorsPolicy, applyCors, corsPreflightResponse } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    const __cors_api: CorsPolicy = { origins: ["https://app.example.com"], allowMethods: ["GET", "HEAD", "OPTIONS"], allowHeaders: ["content-type"], credentials: false, maxAgeSecs: 3600 };
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      if (method === "OPTIONS" && request.headers.get("access-control-request-method") !== null && (path === "/ping" || matchPath("/items/:id", path) !== null)) {
        return corsPreflightResponse(__cors_api, request.headers.get("origin"));
      }
      {
        if ((method === "GET" || method === "HEAD") && path === "/ping") {
          const result = await surface.http_GET_ping();
          const __response = applyCors(httpResultToResponse(result, (v: any) => v as JsonValue), __cors_api, request.headers.get("origin"));
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      {
        const __m = matchPath("/items/:id", path);
        if ((method === "GET" || method === "HEAD") && __m) {
          const __raw_id = __m.params["id"];
          const id = __raw_id;
          const result = await surface.http_GET_items_Param_id(id);
          const __response = applyCors(httpResultToResponse(result, (v: any) => v as JsonValue), __cors_api, request.headers.get("origin"));
          return method === "HEAD" ? headResponse(__response) : __response;
        }
      }
      if (matchPath("/items/:id", path) !== null) {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applyCors(__res, __cors_api, request.headers.get("origin"));
      }
      if (path === "/ping") {
        const __status = method === "OPTIONS" ? 204 : 405;
        const __res = new Response(null, { status: __status, headers: { allow: "GET, HEAD, OPTIONS" } });
        return applyCors(__res, __cors_api, request.headers.get("origin"));
      }
      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
