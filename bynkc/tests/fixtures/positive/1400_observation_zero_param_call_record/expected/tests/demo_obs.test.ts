// Generated by bynkc — do not edit by hand.
// test target: demo.obs

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "../runtime.js";
import * as demo_obs from "./../demo/obs.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function __bynkRecordDeps(deps: any, spec: Record<string, string[]>, obs: { log: Record<string, { args: any[]; order: number }[]>; n: number }): any {
  for (const cap of Object.keys(spec)) {
    if (!deps || !deps[cap]) continue;
    for (const op of spec[cap]) {
      const orig = deps[cap][op];
      if (typeof orig !== "function") continue;
      const key = cap + "." + op;
      obs.log[key] = obs.log[key] ?? [];
      deps[cap][op] = (...args: any[]) => {
        obs.log[key].push({ args, order: obs.n++ });
        return orig.apply(deps[cap], args);
      };
    }
  }
  return deps;
}

type __Clock_now_Call = {  };

function makeTestDeps() {
  return { Clock: new demo_obs.SystemClock() };
}

// case tier: unit
async function test_traces_the_zero_param_clock_read() {
  try {
    const __obs = { log: {} as Record<string, { args: unknown[]; order: number }[]>, n: 0 };
    const deps = __bynkRecordDeps(makeTestDeps(), { Clock: ["now"] }, __obs);
    const { tick } = demo_obs;
    const r = await tick.call(deps);
    if (!(r === 0)) { throw __bynkExpectFailure("tests/obs.test.bynk:8:12", 375, 381, "expect r == 0\n  expected: r == 0\n  actual:   " + __bynkShow((r)) + " == " + __bynkShow((0))); }
    const calls = (((__obs.log["Clock.now"] ?? []).map((__c: { args: unknown[] }) => ({  }))) as __Clock_now_Call[]);
    if (!((calls).length === 1)) { throw __bynkExpectFailure("tests/obs.test.bynk:10:12", 426, 445, "expect calls.length() == 1\n  expected: calls.length() == 1\n  actual:   " + __bynkShow(((calls).length)) + " == " + __bynkShow((1))); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("traces the zero-param clock read")) results.push({ name: "traces the zero-param clock read", ...(await test_traces_the_zero_param_clock_read()) });
  return results;
}
