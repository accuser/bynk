// Generated by bynkc — do not edit by hand.
// Worker entry point for context `demo`.

import { Ok, Err, type Result, type JsonValue, type BoundaryError, boundaryError } from "../../runtime.js";
import { compose, type Env } from "./compose.js";
import * as handlers from "./handlers.js";

export default {
  async fetch(request: Request, env: Env): Promise<Response> {
    const url = new URL(request.url);
    const path = url.pathname;
    const method = request.method;
    const surface = compose(env);
    try {
      if (path.startsWith("/_bynk/call/")) {
        const servicePath = path.slice("/_bynk/call/".length);
        switch (servicePath) {
          case "api": {
            const __contract = request.headers.get("X-Bynk-Contract");
            if (__contract !== "f094bffb7e346b6c") return new Response(JSON.stringify({ kind: "ContractMismatch", service: "api", expected: "f094bffb7e346b6c", actual: __contract }), { status: 409, headers: { "content-type": "application/json" } });
            const args = await request.json() as JsonValue;
            const __r_key = ((__v) => typeof __v === "string" ? Ok(__v) : Err({ kind: "StructuralMismatch", path: "$", expected: "string", actual: typeof __v } as BoundaryError))(args);
            if (__r_key.tag === "Err") return new Response(JSON.stringify(__r_key.error), { status: 400, headers: { "content-type": "application/json" } });
            const key = __r_key.value;
            const result = await surface.api(key);
            const body = null;
            return new Response(JSON.stringify(body), { status: 200, headers: { "content-type": "application/json" } });
          }
          default:
            return new Response("Not found", { status: 404 });
        }
      }

      return new Response("Not Found", { status: 404 });
    } catch {
      return new Response("Internal Server Error", { status: 500 });
    }
  },
};
