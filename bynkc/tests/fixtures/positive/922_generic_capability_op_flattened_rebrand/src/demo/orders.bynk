context demo.orders {
  uses demo.catalog

  consumes idempotency { Idempotency }

  service reserve {
    on call() -> Effect[Option[ReserveOutcome]] given Idempotency {
      let cached <- Idempotency.dedup[ReserveOutcome]("order-key-1")
      cached
    }
  }
}
