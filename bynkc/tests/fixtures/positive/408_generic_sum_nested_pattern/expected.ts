// Generated by bynkc — do not edit by hand.
// commons nested

import { Ok, Err, Some, None, type Result, type Option, type ValidationError } from "./runtime.js";

export type Inner =
    { readonly tag: "Wrap"; readonly payload: string }
  | { readonly tag: "Blank" };

export const Inner = {
  Wrap: (payload: string): Inner => ({ tag: "Wrap", payload }),
  Blank: { tag: "Blank" } as Inner,
};

export type Outer<T> =
    { readonly tag: "Has"; readonly item: T }
  | { readonly tag: "Nothing" };

export const Outer = {
  Has: <T>(item: T): Outer<T> => ({ tag: "Has", item }),
  Nothing: { tag: "Nothing" } as Outer<never>,
};

export function unwrap(o: Outer<Inner>): string {
  if (o.tag === "Has" && o.item.tag === "Wrap") {
    const p = o.item.payload;
    return p;
  }
  if (o.tag === "Has" && o.item.tag === "Blank") {
    return "blank";
  }
  if (o.tag === "Nothing") {
    return "nothing";
  }
  throw new Error("non-exhaustive match");
}

