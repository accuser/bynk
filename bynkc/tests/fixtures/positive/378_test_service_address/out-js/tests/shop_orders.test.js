"use strict";
// Generated by bynkc — do not edit by hand.
// test target: shop.orders
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.run = run;
const shop_orders = __importStar(require("./../shop/orders.js"));
class ExpectationError extends Error {
    location;
    start;
    end;
    constructor(location, start, end, detail) {
        super(`${detail}\n  at ${location}`);
        this.location = location;
        this.start = start;
        this.end = end;
    }
}
function __bynkExpectFailure(location, start, end, detail) {
    return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond, location, start, end, detail) {
    if (!cond) {
        throw __bynkExpectFailure(location, start, end, detail);
    }
}
function __bynkShow(v) {
    try {
        return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v));
    }
    catch {
        return String(v);
    }
}
function makeTestDeps() {
    return {};
}
// case tier: unit
async function test_a_buyer_s_cart_is_their_own() {
    try {
        shop_orders.__resetAgents();
        const deps = makeTestDeps();
        const { Cart, Item, UserId, __makeCart, api, intake, sweeper } = shop_orders;
        const __r0 = await api.http_POST_cart_Param_sku("widget", { ...deps, identity: "alice" });
        const mine = await api.http_GET_cart_size({ ...deps, identity: "bob" });
        if (!(mine.tag === "Ok")) {
            throw __bynkExpectFailure("shop/tests/orders.test.bynk:5:12", 191, 204, "expect mine is Ok(_)");
        }
        return { pass: true };
    }
    catch (e) {
        if (e instanceof ExpectationError) {
            return { pass: false, error: { message: e.message, location: e.location } };
        }
        return { pass: false, error: { message: String(e), location: "unknown" } };
    }
}
// case tier: unit
async function test_the_sweeper_runs_clean() {
    try {
        shop_orders.__resetAgents();
        const deps = makeTestDeps();
        const { Cart, Item, UserId, __makeCart, api, intake, sweeper } = shop_orders;
        const r = await sweeper.cron_sweeper_0(deps);
        if (!(r.tag === "Ok")) {
            throw __bynkExpectFailure("shop/tests/orders.test.bynk:10:12", 298, 308, "expect r is Ok(_)");
        }
        return { pass: true };
    }
    catch (e) {
        if (e instanceof ExpectationError) {
            return { pass: false, error: { message: e.message, location: e.location } };
        }
        return { pass: false, error: { message: String(e), location: "unknown" } };
    }
}
// case tier: unit
async function test_an_intake_message_is_acked() {
    try {
        shop_orders.__resetAgents();
        const deps = makeTestDeps();
        const { Cart, Item, UserId, __makeCart, api, intake, sweeper } = shop_orders;
        const r = await intake.queue_intake_0({ sku: "x" }, deps);
        if (!(r.tag === "Ack")) {
            throw __bynkExpectFailure("shop/tests/orders.test.bynk:15:12", 410, 418, "expect r is Ack");
        }
        return { pass: true };
    }
    catch (e) {
        if (e instanceof ExpectationError) {
            return { pass: false, error: { message: e.message, location: e.location } };
        }
        return { pass: false, error: { message: String(e), location: "unknown" } };
    }
}
async function run(only) {
    const results = [];
    const want = (n) => only === undefined || only === n;
    if (want("a buyer's cart is their own"))
        results.push({ name: "a buyer's cart is their own", ...(await test_a_buyer_s_cart_is_their_own()) });
    if (want("the sweeper runs clean"))
        results.push({ name: "the sweeper runs clean", ...(await test_the_sweeper_runs_clean()) });
    if (want("an intake message is acked"))
        results.push({ name: "an intake message is acked", ...(await test_an_intake_message_is_acked()) });
    return results;
}
//# sourceMappingURL=shop_orders.test.ts.map
