// Generated by bynkc — do not edit by hand.
// test target: shop.orders

import { Ok, Err, Some, None, makeTestState, type Result, type Option, type ValidationError } from "../runtime.js";
import * as shop_orders from "./../shop/orders.js";

class ExpectationError extends Error {
  location: string;
  start: number;
  end: number;
  constructor(location: string, start: number, end: number, detail: string) {
    super(`${detail}\n  at ${location}`);
    this.location = location;
    this.start = start;
    this.end = end;
  }
}
function __bynkExpectFailure(location: string, start: number, end: number, detail: string) {
  return new ExpectationError(location, start, end, detail);
}
function __bynkExpect(cond: boolean, location: string, start: number, end: number, detail: string): void {
  if (!cond) { throw __bynkExpectFailure(location, start, end, detail); }
}
function __bynkShow(v: unknown): string {
  try { return typeof v === "bigint" ? String(v) : (JSON.stringify(v) ?? String(v)); } catch { return String(v); }
}

function makeTestDeps() {
  return {  };
}

// case tier: unit
async function test_a_buyer_s_cart_is_their_own() {
  try {
    shop_orders.__resetAgents();
    const deps = makeTestDeps();
    const { Cart, Item, UserId, __makeCart, api, intake, sweeper } = shop_orders as any;
    const __r0 = await api.http_POST_cart_Param_sku("widget", { ...deps, identity: (("alice" as any) as any) });
    const mine = await api.http_GET_cart_size({ ...deps, identity: (("bob" as any) as any) });
    if (!(mine.tag === "Ok")) { throw __bynkExpectFailure("shop/tests/orders.test.bynk:5:12", 191, 204, "expect mine is Ok(_)"); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_the_sweeper_runs_clean() {
  try {
    shop_orders.__resetAgents();
    const deps = makeTestDeps();
    const { Cart, Item, UserId, __makeCart, api, intake, sweeper } = shop_orders as any;
    const r = await sweeper.cron_sweeper_0(deps);
    if (!(r.tag === "Ok")) { throw __bynkExpectFailure("shop/tests/orders.test.bynk:10:12", 298, 308, "expect r is Ok(_)"); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

// case tier: unit
async function test_an_intake_message_is_acked() {
  try {
    shop_orders.__resetAgents();
    const deps = makeTestDeps();
    const { Cart, Item, UserId, __makeCart, api, intake, sweeper } = shop_orders as any;
    const r = await intake.queue_intake_0({ sku: "x" }, deps);
    if (!(r.tag === "Ack")) { throw __bynkExpectFailure("shop/tests/orders.test.bynk:15:12", 410, 418, "expect r is Ack"); }
    return { pass: true };
  } catch (e) {
    if (e instanceof ExpectationError) {
      return { pass: false, error: { message: e.message, location: e.location } };
    }
    return { pass: false, error: { message: String(e), location: "unknown" } };
  }
}

export async function run(only?: string) {
  const results = [];
  const want = (n: string): boolean => only === undefined || only === n;
  if (want("a buyer's cart is their own")) results.push({ name: "a buyer's cart is their own", ...(await test_a_buyer_s_cart_is_their_own()) });
  if (want("the sweeper runs clean")) results.push({ name: "the sweeper runs clean", ...(await test_the_sweeper_runs_clean()) });
  if (want("an intake message is acked")) results.push({ name: "an intake message is acked", ...(await test_an_intake_message_is_acked()) });
  return results;
}
//# sourceMappingURL=shop_orders.test.ts.map
