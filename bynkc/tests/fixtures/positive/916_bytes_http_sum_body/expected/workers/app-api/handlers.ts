// Generated by bynkc — do not edit by hand.
// context app.api

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, HttpResult, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../../runtime.js";

export type UserId = string & { readonly __brand: "app.api.UserId" };

export const UserId = {
  of(value: string): Result<UserId, ValidationError> {
    if (!(value.length > 0)) {
      return Err({ field: "UserId", message: "must be non-empty", value });
    }
    return Ok(value as UserId);
  },
};

export const api = {
  async http_POST_blobs(body: Uint8Array, deps: { who: { tag: "User", identity: UserId } | { tag: "Visitor" } }): Promise<HttpResult<number>> {
    switch (deps.who.tag) {
      case "User": {
        return HttpResult.Ok((body).length);
      }
      case "Visitor": {
        return HttpResult.Ok(0);
      }
    }
    throw new Error("non-exhaustive match");
  },
};

