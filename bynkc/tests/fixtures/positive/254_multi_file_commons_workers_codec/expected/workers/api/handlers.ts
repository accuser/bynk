// Generated by bynkc — do not edit by hand.
// context api

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../../runtime.js";

import { Cents as __CommonsCents } from "../../money/cents.js";

export type Cents = __CommonsCents & { readonly __ctxBrand: "api" };
export const Cents = {
  of(value: number): Result<Cents, ValidationError> { return __CommonsCents.of(value) as unknown as Result<Cents, ValidationError>; },
  unsafe(value: number): Cents { return __CommonsCents.unsafe(value) as unknown as Cents; },
};

/**
 * Regression for #479: a multi-file commons (`money`, split across `cents.bynk` +
 * `label.bynk`) whose refined type crosses a cross-Worker boundary. Under
 * `--target workers` the boundary codec (`serialise_Cents`/`deserialise_Cents`)
 * must be emitted exactly once, in the file that declares `Cents` (`cents.ts`) —
 * never orphaned into the sibling `label.ts`, which has neither the type nor its
 * imports in scope.
 */

export const check = {
  async call(c: Cents, deps: {}): Promise<number> {
    return 0;
  },
};

import { serialise_Cents, deserialise_Cents } from "../../money/cents.js";
export { serialise_Cents, deserialise_Cents };

