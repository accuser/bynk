// Generated by bynkc — do not edit by hand.
// commons money

import { Ok, Err, Some, None, type Result, type Option, type ValidationError, type JsonValue, type BoundaryError, type ServiceBinding, callService, boundaryError } from "../runtime.js";

export type Cents = number & { readonly __brand: "Cents" };

export const Cents = {
  of(value: number): Result<Cents, ValidationError> {
    if (!Number.isInteger(value)) {
      return Err({ field: "Cents", message: "must be an integer", value });
    }
    if (!(value >= 0)) {
      return Err({ field: "Cents", message: "must be non-negative", value });
    }
    return Ok(value as Cents);
  },
  unsafe(value: number): Cents {
    return value as Cents;
  },
  fromInt(n: number): Result<Cents, ValidationError> {
    return Cents.of(n);
  },
  equals(self: Cents, other: Cents): boolean {
    return self === other;
  },
};

export function serialise_Cents(value: Cents): JsonValue {
  return value as unknown as number;
}

export function deserialise_Cents(json: JsonValue, path: string = "$"): Result<Cents, BoundaryError> {
  if (typeof json !== "number") {
    return Err({ kind: "StructuralMismatch", path, expected: "number", actual: typeof json });
  }
  const validated = (typeof (Cents as any).of === "function")
    ? (Cents as any).of(json)
    : Ok(json as unknown as Cents);
  if (validated.tag === "Err") {
    return Err({ kind: "RefinementViolation", path, violation: validated.error });
  }
  return Ok(validated.value as Cents);
}


