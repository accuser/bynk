context commerce.baskets

type CustomerId = opaque String where NonEmpty
type Sku = opaque String where NonEmpty
type Quantity = Int where InRange(1, 100)

type BasketSnapshot = {
  lineCount: Int,
  revision: Int,
  note: Option[String],
}
