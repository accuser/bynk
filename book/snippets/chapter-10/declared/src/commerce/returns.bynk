context commerce.returns

consumes commerce.inventory as Inventory

fn eligible(sku: String) -> Bool { sku != "" }

service accept {
	on call(sku: String) -> Effect[Bool] {
		if eligible(sku) {
			let released <- Inventory.release(sku)
			released
		} else { false }
	}
}
