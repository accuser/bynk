context commerce.payment

exports transparent { PaymentError }

type PaymentError = enum { Declined }

service authorise {
  on call(cents: Int) -> Effect[Result[Int, PaymentError]] {
    if cents > 100000 { Err(Declined) } else { Ok(cents) }
  }
}
