context support.dashboard

consumes support.counters as Counters
consumes text.normalise { Slug }

service refresh {
	on call(label: String) -> Effect[Result[Int, ()]] given Slug {
		let name <- Slug.make(label)
		let result <- Counters.increment(name, 1)
		result
	}
}
