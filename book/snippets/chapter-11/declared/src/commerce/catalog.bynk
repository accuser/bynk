context commerce.catalog

consumes commerce.metrics as Metrics
consumes text.normalise { Slug }

service refresh {
	on call(label: String) -> Effect[Result[Int, ()]] given Slug {
		let name <- Slug.make(label)
		let result <- Metrics.increment(name, 1)
		result
	}
}
