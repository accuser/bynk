commons shop.values

type OrderId = String where NonEmpty && MaxLength(64)

type CustomerId = String where NonEmpty && MaxLength(64)

type Sku = String where NonEmpty && MaxLength(64)

type Quantity = Int where Positive

type Cents = Int where Positive

type OrderRequest = { sku: Sku, quantity: Quantity, cents: Cents }
