[package]
name = "bynk-lower"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Bynk's &CheckedProgram -> Ir lowering pass: the AST-analysis helpers bynk-emit calls to build bynk-ir's declaration-level values (handler kinds, service protocols, store-field shapes, capability signatures, event-subscriber shapes). Carved out of bynk-emit alongside bynk-ir (ADR 0332's deferred split, ADR 0385's trigger)."
keywords = ["compiler", "dsl", "ir", "language"]
categories = ["compilers"]

[lib]
path = "src/lib.rs"

[dependencies]
bynk-syntax.workspace = true
bynk-check.workspace = true
bynk-ir.workspace = true

[dev-dependencies]
bynk-project.workspace = true
